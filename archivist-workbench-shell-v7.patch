diff --git a/frontend/src/App.tsx b/frontend/src/App.tsx
index 6b39524..5631a22 100644
--- a/frontend/src/App.tsx
+++ b/frontend/src/App.tsx
@@ -153,6 +153,27 @@ export function App() {
     return agents.find((agent) => agent.id === activeAgentId) ?? null;
   }, [activeAgentId, agents]);
 
+  const chatDockPanelWidth = useMemo(() => {
+    const labels =
+      chatDockView === "chats"
+        ? [...chats, ...archivedChats].map((chat) => chat.title)
+        : chatDockView === "agents"
+          ? [...agents, ...archivedAgents].map((agent) => agent.name)
+          : [];
+
+    const longestLabelLength = labels.reduce(
+      (longest, label) => Math.max(longest, label.trim().length),
+      0,
+    );
+
+    return Math.round(
+      Math.min(
+        202,
+        Math.max(148, 108 + Math.min(longestLabelLength, 23) * 4.1),
+      ),
+    );
+  }, [agents, archivedAgents, archivedChats, chatDockView, chats]);
+
   const managedAgent = useMemo(() => {
     return (
       agents.find((agent) => agent.id === managedAgentId) ??
@@ -644,9 +665,7 @@ export function App() {
     }
   }
 
-  function handleToggleChatDockView(
-    nextView: Exclude<ChatDockView, null>,
-  ) {
+  function handleToggleChatDockView(nextView: Exclude<ChatDockView, null>) {
     setChatDockView((current) => (current === nextView ? null : nextView));
   }
 
@@ -846,9 +865,35 @@ export function App() {
             }
             controlPanelLabel={
               chatDockView === "chats"
-                ? "Chat browser"
+                ? "Chats"
+                : chatDockView === "agents"
+                  ? "Agents"
+                  : null
+            }
+            controlPanelWidth={chatDockPanelWidth}
+            controlPanelActionLabel={
+              chatDockView === "chats"
+                ? addingChat
+                  ? "Creating Chat..."
+                  : "New Chat"
+                : chatDockView === "agents"
+                  ? addingAgent
+                    ? "Creating Agent..."
+                    : "New Agent"
+                  : null
+            }
+            controlPanelActionBusy={
+              chatDockView === "chats"
+                ? addingChat
+                : chatDockView === "agents"
+                  ? addingAgent
+                  : false
+            }
+            onControlPanelAction={
+              chatDockView === "chats"
+                ? handleAddChat
                 : chatDockView === "agents"
-                  ? "Agent browser"
+                  ? handleAddAgent
                   : null
             }
             controlPanel={
@@ -859,8 +904,6 @@ export function App() {
                     archivedChats={archivedChats}
                     selectedChatId={selectedChatId}
                     loading={loadingChats}
-                    adding={addingChat}
-                    onAddChat={handleAddChat}
                     onSelectChat={handleSelectChat}
                     onManageChat={setManagedChatId}
                     onManageArchivedChat={setManagedChatId}
@@ -873,8 +916,6 @@ export function App() {
                     archivedAgents={archivedAgents}
                     activeAgentId={activeAgentId}
                     loading={loadingAgents}
-                    adding={addingAgent}
-                    onAddAgent={handleAddAgent}
                     onSelectAgent={handleSelectAgent}
                     onManageAgent={setManagedAgentId}
                     onManageArchivedAgent={setManagedAgentId}
diff --git a/frontend/src/components/chat/ChatWindow.module.css b/frontend/src/components/chat/ChatWindow.module.css
index 2c01d00..82244e2 100644
--- a/frontend/src/components/chat/ChatWindow.module.css
+++ b/frontend/src/components/chat/ChatWindow.module.css
@@ -169,13 +169,12 @@
   grid-row: 2;
   z-index: 5;
   display: grid;
-  grid-template-rows: var(--chat-dock-header-height)
+  grid-template-rows:
+    var(--chat-dock-header-height)
     var(--chat-dock-body-height);
   justify-self: center;
   width: min(calc(100% - 32px), 1120px);
-  height: calc(
-    var(--chat-dock-header-height) + var(--chat-dock-body-height)
-  );
+  height: calc(var(--chat-dock-header-height) + var(--chat-dock-body-height));
   min-width: 0;
   min-height: 0;
   margin: 0 16px 12px;
@@ -236,7 +235,9 @@
 }
 
 .composerBodyWithPanel {
-  grid-template-columns: minmax(0, 1fr) clamp(230px, 25vw, 276px);
+  grid-template-columns:
+    minmax(0, 1fr)
+    min(var(--chat-control-panel-width, 176px), 31vw);
 }
 
 .composer {
@@ -376,7 +377,7 @@
 
 .controlPanel {
   display: grid;
-  grid-template-rows: 27px minmax(0, 1fr);
+  grid-template-rows: 25px minmax(0, 1fr);
   width: 100%;
   height: 100%;
   min-width: 0;
@@ -393,8 +394,8 @@
   align-items: center;
   justify-content: space-between;
   min-width: 0;
-  gap: 8px;
-  padding: 0 8px;
+  gap: 5px;
+  padding: 0 4px 0 7px;
   border-bottom: 1px solid rgba(214, 198, 170, 0.07);
   color: var(--app-muted-text);
   background: rgba(255, 255, 255, 0.012);
@@ -404,7 +405,7 @@
   min-width: 0;
   overflow: hidden;
   color: var(--app-text);
-  font-size: 9px;
+  font-size: 8.5px;
   font-weight: 700;
   letter-spacing: 0.06em;
   text-overflow: ellipsis;
@@ -412,10 +413,31 @@
   white-space: nowrap;
 }
 
-.controlPanelHeader span {
+.controlPanelAction {
+  display: grid;
+  place-items: center;
   flex: 0 0 auto;
-  font-size: 8px;
-  opacity: 0.64;
+  width: 20px;
+  height: 20px;
+  padding: 0;
+  border: 1px solid transparent;
+  border-radius: 4px;
+  color: var(--app-muted-text);
+  background: transparent;
+  cursor: pointer;
+}
+
+.controlPanelAction:hover:not(:disabled),
+.controlPanelAction:focus-visible:not(:disabled) {
+  border-color: rgba(143, 122, 223, 0.22);
+  color: var(--app-text);
+  background: rgba(143, 122, 223, 0.1);
+  outline: none;
+}
+
+.controlPanelAction:disabled {
+  cursor: default;
+  opacity: 0.4;
 }
 
 .controlPanelContent {
@@ -475,7 +497,9 @@
   }
 
   .composerBodyWithPanel {
-    grid-template-columns: minmax(0, 1fr) minmax(208px, 43vw);
+    grid-template-columns:
+      minmax(0, 1fr)
+      min(var(--chat-control-panel-width, 176px), 38vw);
   }
 
   .keyboardHint {
@@ -489,12 +513,13 @@
   }
 
   .composerBodyWithPanel {
-    grid-template-columns: minmax(0, 1fr) minmax(190px, 48vw);
+    grid-template-columns:
+      minmax(0, 1fr)
+      min(var(--chat-control-panel-width, 176px), 46vw);
   }
 
   .composerTab span,
-  .composerButton span,
-  .controlPanelHeader span {
+  .composerButton span {
     display: none;
   }
 
diff --git a/frontend/src/components/chat/ChatWindow.tsx b/frontend/src/components/chat/ChatWindow.tsx
index aca73a4..9872387 100644
--- a/frontend/src/components/chat/ChatWindow.tsx
+++ b/frontend/src/components/chat/ChatWindow.tsx
@@ -1,4 +1,5 @@
 import {
+  type CSSProperties,
   type FormEvent,
   type KeyboardEvent,
   type ReactNode,
@@ -11,6 +12,7 @@ import {
   Code2,
   List,
   MessageSquareText,
+  Plus,
   SendHorizontal,
   Sparkles,
   TextQuote,
@@ -24,6 +26,10 @@ type ChatWindowProps = {
   toolbar: ReactNode;
   controlPanel: ReactNode | null;
   controlPanelLabel: string | null;
+  controlPanelWidth?: number;
+  controlPanelActionLabel?: string | null;
+  controlPanelActionBusy?: boolean;
+  onControlPanelAction?: (() => void) | null;
   onChatActivity: (chatId: string) => void;
 };
 
@@ -66,6 +72,10 @@ export function ChatWindow({
   toolbar,
   controlPanel,
   controlPanelLabel,
+  controlPanelWidth = 176,
+  controlPanelActionLabel = null,
+  controlPanelActionBusy = false,
+  onControlPanelAction = null,
   onChatActivity,
 }: ChatWindowProps) {
   const [messages, setMessages] = useState<ChatMessage[]>([]);
@@ -355,6 +365,13 @@ export function ChatWindow({
           className={`${styles.composerBody} ${
             controlPanel ? styles.composerBodyWithPanel : ""
           }`}
+          style={
+            controlPanel
+              ? ({
+                  "--chat-control-panel-width": `${controlPanelWidth}px`,
+                } as CSSProperties)
+              : undefined
+          }
         >
           <form className={styles.composer} onSubmit={onSubmit}>
             <div className={styles.composerInner}>
@@ -432,7 +449,19 @@ export function ChatWindow({
             >
               <div className={styles.controlPanelHeader}>
                 <strong>{controlPanelLabel ?? "Chat controls"}</strong>
-                <span>Scroll to browse</span>
+
+                {onControlPanelAction && controlPanelActionLabel ? (
+                  <button
+                    className={styles.controlPanelAction}
+                    type="button"
+                    onClick={onControlPanelAction}
+                    disabled={controlPanelActionBusy}
+                    aria-label={controlPanelActionLabel}
+                    title={controlPanelActionLabel}
+                  >
+                    <Plus size={12} strokeWidth={2.2} />
+                  </button>
+                ) : null}
               </div>
 
               <div className={styles.controlPanelContent}>{controlPanel}</div>
diff --git a/frontend/src/components/sidebar/Agents/Agents.module.css b/frontend/src/components/sidebar/Agents/Agents.module.css
index 26f092f..6c1c63c 100644
--- a/frontend/src/components/sidebar/Agents/Agents.module.css
+++ b/frontend/src/components/sidebar/Agents/Agents.module.css
@@ -6,52 +6,18 @@
   min-height: 0;
 }
 
-.toolbar {
-  flex: 0 0 auto;
-  padding: 4px;
-}
-
-.createButton {
-  display: flex;
-  align-items: center;
-  justify-content: center;
-  gap: 6px;
-  width: 100%;
-  min-height: 29px;
-  padding: 0 8px;
-  border: 1px solid rgba(143, 122, 223, 0.14);
-  border-radius: 4px;
-  color: var(--app-text);
-  background: rgba(255, 255, 255, 0.045);
-  font: inherit;
-  font-size: 11px;
-  cursor: pointer;
-}
-
-.createButton:hover:not(:disabled),
-.createButton:focus-visible:not(:disabled) {
-  border-color: rgba(143, 122, 223, 0.3);
-  background: rgba(143, 122, 223, 0.1);
-  outline: none;
-}
-
-.createButton:disabled {
-  cursor: default;
-  opacity: 0.45;
-}
-
 .listViewport {
   flex: 1;
   min-height: 0;
   overflow-y: auto;
-  padding: 2px 4px 6px;
+  padding: 3px 3px 5px;
   scrollbar-gutter: stable;
 }
 
 .list {
   display: flex;
   flex-direction: column;
-  gap: 2px;
+  gap: 1px;
   margin: 0;
   padding: 0;
   list-style: none;
@@ -65,21 +31,26 @@
 .row {
   display: flex;
   align-items: center;
-  gap: 7px;
+  gap: 6px;
   width: 100%;
-  min-height: 31px;
-  padding: 0 28px 0 7px;
+  min-height: 27px;
+  padding: 0 24px 0 6px;
   border: 1px solid transparent;
   border-radius: 4px;
   color: var(--app-muted-text);
   background: transparent;
   font: inherit;
-  font-size: 11px;
+  font-size: 10px;
   text-align: left;
   cursor: pointer;
 }
 
+.row svg {
+  flex: 0 0 auto;
+}
+
 .row span {
+  min-width: 0;
   overflow: hidden;
   text-overflow: ellipsis;
   white-space: nowrap;
@@ -104,8 +75,8 @@
   right: 3px;
   display: grid;
   place-items: center;
-  width: 24px;
-  height: 24px;
+  width: 21px;
+  height: 21px;
   padding: 0;
   border: 0;
   border-radius: 4px;
@@ -132,15 +103,15 @@
   display: flex;
   align-items: center;
   flex: 0 0 auto;
-  gap: 5px;
-  min-height: 29px;
-  padding: 0 7px;
+  gap: 4px;
+  min-height: 25px;
+  padding: 0 6px;
   border: 0;
   border-top: 1px solid rgba(214, 198, 170, 0.08);
   color: var(--app-muted-text);
   background: rgba(0, 0, 0, 0.08);
   font: inherit;
-  font-size: 10px;
+  font-size: 9px;
   cursor: pointer;
 }
 
@@ -165,9 +136,9 @@
 
 .archivedList {
   flex: 0 0 auto;
-  max-height: 150px;
+  max-height: 112px;
   overflow-y: auto;
-  padding: 3px 4px 5px;
+  padding: 2px 3px 4px;
   border-top: 1px solid rgba(214, 198, 170, 0.06);
   background: rgba(0, 0, 0, 0.14);
 }
@@ -175,21 +146,22 @@
 .archivedRow {
   display: flex;
   align-items: center;
-  gap: 6px;
+  gap: 5px;
   width: 100%;
-  min-height: 27px;
-  padding: 0 7px;
+  min-height: 24px;
+  padding: 0 6px;
   border: 0;
   border-radius: 3px;
   color: var(--app-muted-text);
   background: transparent;
   font: inherit;
-  font-size: 10px;
+  font-size: 9px;
   text-align: left;
   cursor: pointer;
 }
 
 .archivedRow span {
+  min-width: 0;
   overflow: hidden;
   text-overflow: ellipsis;
   white-space: nowrap;
@@ -204,9 +176,9 @@
 
 .empty,
 .archivedEmpty {
-  padding: 10px;
+  padding: 8px;
   color: var(--app-muted-text);
-  font-size: 10px;
+  font-size: 9px;
 }
 
 @media (prefers-reduced-motion: reduce) {
diff --git a/frontend/src/components/sidebar/Agents/Agents.tsx b/frontend/src/components/sidebar/Agents/Agents.tsx
index 7832446..d1b6e07 100644
--- a/frontend/src/components/sidebar/Agents/Agents.tsx
+++ b/frontend/src/components/sidebar/Agents/Agents.tsx
@@ -1,12 +1,5 @@
 import { useState } from "react";
-import {
-  Archive,
-  Bot,
-  ChevronRight,
-  Pencil,
-  Plus,
-  ShieldCheck,
-} from "lucide-react";
+import { Archive, Bot, ChevronRight, Pencil, ShieldCheck } from "lucide-react";
 import type { Agent } from "../../../domains/agent/agent.types";
 import styles from "./Agents.module.css";
 
@@ -15,8 +8,6 @@ type AgentsProps = {
   archivedAgents: Agent[];
   activeAgentId: string | null;
   loading: boolean;
-  adding: boolean;
-  onAddAgent: () => void;
   onSelectAgent: (agentId: string) => void;
   onManageAgent: (agentId: string) => void;
   onManageArchivedAgent: (agentId: string) => void;
@@ -27,8 +18,6 @@ export function Agents({
   archivedAgents,
   activeAgentId,
   loading,
-  adding,
-  onAddAgent,
   onSelectAgent,
   onManageAgent,
   onManageArchivedAgent,
@@ -37,18 +26,6 @@ export function Agents({
 
   return (
     <section className={styles.section}>
-      <div className={styles.toolbar}>
-        <button
-          className={styles.createButton}
-          type="button"
-          onClick={onAddAgent}
-          disabled={adding}
-        >
-          <Plus size={14} strokeWidth={2.2} />
-          <span>{adding ? "Creating..." : "New Agent"}</span>
-        </button>
-      </div>
-
       <div className={styles.listViewport}>
         {loading ? (
           <div className={styles.empty}>Loading Agents...</div>
@@ -66,9 +43,9 @@ export function Agents({
                     title={`Use ${agent.name}`}
                   >
                     {agent.isBuiltIn ? (
-                      <ShieldCheck size={13} strokeWidth={2} />
+                      <ShieldCheck size={12} strokeWidth={2} />
                     ) : (
-                      <Bot size={13} strokeWidth={2} />
+                      <Bot size={12} strokeWidth={2} />
                     )}
                     <span>{agent.name}</span>
                   </button>
@@ -80,7 +57,7 @@ export function Agents({
                     aria-label={`Manage ${agent.name}`}
                     title="Manage Agent"
                   >
-                    <Pencil size={12} strokeWidth={2.1} />
+                    <Pencil size={11} strokeWidth={2.1} />
                   </button>
                 </li>
               );
@@ -97,11 +74,11 @@ export function Agents({
         onClick={() => setArchivedOpen((current) => !current)}
       >
         <ChevronRight
-          size={12}
+          size={11}
           strokeWidth={2.2}
           className={archivedOpen ? styles.caretOpen : ""}
         />
-        <Archive size={12} strokeWidth={2} />
+        <Archive size={11} strokeWidth={2} />
         <span>Archived</span>
         <span className={styles.count}>{archivedAgents.length}</span>
       </button>
@@ -117,7 +94,7 @@ export function Agents({
                 onClick={() => onManageArchivedAgent(agent.id)}
                 title={agent.name}
               >
-                <Archive size={12} strokeWidth={2} />
+                <Archive size={11} strokeWidth={2} />
                 <span>{agent.name}</span>
               </button>
             ))
diff --git a/frontend/src/components/sidebar/Chats/Chats.module.css b/frontend/src/components/sidebar/Chats/Chats.module.css
index 26f092f..6c1c63c 100644
--- a/frontend/src/components/sidebar/Chats/Chats.module.css
+++ b/frontend/src/components/sidebar/Chats/Chats.module.css
@@ -6,52 +6,18 @@
   min-height: 0;
 }
 
-.toolbar {
-  flex: 0 0 auto;
-  padding: 4px;
-}
-
-.createButton {
-  display: flex;
-  align-items: center;
-  justify-content: center;
-  gap: 6px;
-  width: 100%;
-  min-height: 29px;
-  padding: 0 8px;
-  border: 1px solid rgba(143, 122, 223, 0.14);
-  border-radius: 4px;
-  color: var(--app-text);
-  background: rgba(255, 255, 255, 0.045);
-  font: inherit;
-  font-size: 11px;
-  cursor: pointer;
-}
-
-.createButton:hover:not(:disabled),
-.createButton:focus-visible:not(:disabled) {
-  border-color: rgba(143, 122, 223, 0.3);
-  background: rgba(143, 122, 223, 0.1);
-  outline: none;
-}
-
-.createButton:disabled {
-  cursor: default;
-  opacity: 0.45;
-}
-
 .listViewport {
   flex: 1;
   min-height: 0;
   overflow-y: auto;
-  padding: 2px 4px 6px;
+  padding: 3px 3px 5px;
   scrollbar-gutter: stable;
 }
 
 .list {
   display: flex;
   flex-direction: column;
-  gap: 2px;
+  gap: 1px;
   margin: 0;
   padding: 0;
   list-style: none;
@@ -65,21 +31,26 @@
 .row {
   display: flex;
   align-items: center;
-  gap: 7px;
+  gap: 6px;
   width: 100%;
-  min-height: 31px;
-  padding: 0 28px 0 7px;
+  min-height: 27px;
+  padding: 0 24px 0 6px;
   border: 1px solid transparent;
   border-radius: 4px;
   color: var(--app-muted-text);
   background: transparent;
   font: inherit;
-  font-size: 11px;
+  font-size: 10px;
   text-align: left;
   cursor: pointer;
 }
 
+.row svg {
+  flex: 0 0 auto;
+}
+
 .row span {
+  min-width: 0;
   overflow: hidden;
   text-overflow: ellipsis;
   white-space: nowrap;
@@ -104,8 +75,8 @@
   right: 3px;
   display: grid;
   place-items: center;
-  width: 24px;
-  height: 24px;
+  width: 21px;
+  height: 21px;
   padding: 0;
   border: 0;
   border-radius: 4px;
@@ -132,15 +103,15 @@
   display: flex;
   align-items: center;
   flex: 0 0 auto;
-  gap: 5px;
-  min-height: 29px;
-  padding: 0 7px;
+  gap: 4px;
+  min-height: 25px;
+  padding: 0 6px;
   border: 0;
   border-top: 1px solid rgba(214, 198, 170, 0.08);
   color: var(--app-muted-text);
   background: rgba(0, 0, 0, 0.08);
   font: inherit;
-  font-size: 10px;
+  font-size: 9px;
   cursor: pointer;
 }
 
@@ -165,9 +136,9 @@
 
 .archivedList {
   flex: 0 0 auto;
-  max-height: 150px;
+  max-height: 112px;
   overflow-y: auto;
-  padding: 3px 4px 5px;
+  padding: 2px 3px 4px;
   border-top: 1px solid rgba(214, 198, 170, 0.06);
   background: rgba(0, 0, 0, 0.14);
 }
@@ -175,21 +146,22 @@
 .archivedRow {
   display: flex;
   align-items: center;
-  gap: 6px;
+  gap: 5px;
   width: 100%;
-  min-height: 27px;
-  padding: 0 7px;
+  min-height: 24px;
+  padding: 0 6px;
   border: 0;
   border-radius: 3px;
   color: var(--app-muted-text);
   background: transparent;
   font: inherit;
-  font-size: 10px;
+  font-size: 9px;
   text-align: left;
   cursor: pointer;
 }
 
 .archivedRow span {
+  min-width: 0;
   overflow: hidden;
   text-overflow: ellipsis;
   white-space: nowrap;
@@ -204,9 +176,9 @@
 
 .empty,
 .archivedEmpty {
-  padding: 10px;
+  padding: 8px;
   color: var(--app-muted-text);
-  font-size: 10px;
+  font-size: 9px;
 }
 
 @media (prefers-reduced-motion: reduce) {
diff --git a/frontend/src/components/sidebar/Chats/Chats.tsx b/frontend/src/components/sidebar/Chats/Chats.tsx
index a87bc0e..78c6ddd 100644
--- a/frontend/src/components/sidebar/Chats/Chats.tsx
+++ b/frontend/src/components/sidebar/Chats/Chats.tsx
@@ -1,11 +1,5 @@
 import { useState } from "react";
-import {
-  Archive,
-  ChevronRight,
-  MessageSquareText,
-  Pencil,
-  Plus,
-} from "lucide-react";
+import { Archive, ChevronRight, MessageSquareText, Pencil } from "lucide-react";
 import type { Chat } from "../../../domains/chat/chat.types";
 import styles from "./Chats.module.css";
 
@@ -14,8 +8,6 @@ type ChatsProps = {
   archivedChats: Chat[];
   selectedChatId: string | null;
   loading: boolean;
-  adding: boolean;
-  onAddChat: () => void;
   onSelectChat: (chatId: string) => void;
   onManageChat: (chatId: string) => void;
   onManageArchivedChat: (chatId: string) => void;
@@ -26,8 +18,6 @@ export function Chats({
   archivedChats,
   selectedChatId,
   loading,
-  adding,
-  onAddChat,
   onSelectChat,
   onManageChat,
   onManageArchivedChat,
@@ -36,18 +26,6 @@ export function Chats({
 
   return (
     <section className={styles.section}>
-      <div className={styles.toolbar}>
-        <button
-          className={styles.createButton}
-          type="button"
-          onClick={onAddChat}
-          disabled={adding}
-        >
-          <Plus size={14} strokeWidth={2.2} />
-          <span>{adding ? "Creating..." : "New Chat"}</span>
-        </button>
-      </div>
-
       <div className={styles.listViewport}>
         {loading ? (
           <div className={styles.empty}>Loading chats...</div>
@@ -64,7 +42,7 @@ export function Chats({
                     onClick={() => onSelectChat(chat.id)}
                     title={chat.title}
                   >
-                    <MessageSquareText size={13} strokeWidth={2} />
+                    <MessageSquareText size={12} strokeWidth={2} />
                     <span>{chat.title}</span>
                   </button>
 
@@ -75,7 +53,7 @@ export function Chats({
                     aria-label={`Manage ${chat.title}`}
                     title="Manage Chat"
                   >
-                    <Pencil size={12} strokeWidth={2.1} />
+                    <Pencil size={11} strokeWidth={2.1} />
                   </button>
                 </li>
               );
@@ -92,11 +70,11 @@ export function Chats({
         onClick={() => setArchivedOpen((current) => !current)}
       >
         <ChevronRight
-          size={12}
+          size={11}
           strokeWidth={2.2}
           className={archivedOpen ? styles.caretOpen : ""}
         />
-        <Archive size={12} strokeWidth={2} />
+        <Archive size={11} strokeWidth={2} />
         <span>Archived</span>
         <span className={styles.count}>{archivedChats.length}</span>
       </button>
@@ -112,7 +90,7 @@ export function Chats({
                 onClick={() => onManageArchivedChat(chat.id)}
                 title={chat.title}
               >
-                <Archive size={12} strokeWidth={2} />
+                <Archive size={11} strokeWidth={2} />
                 <span>{chat.title}</span>
               </button>
             ))
diff --git a/frontend/src/components/sidebar/Libraries/LibraryFileTree/LibraryFileTree.tsx b/frontend/src/components/sidebar/Libraries/LibraryFileTree/LibraryFileTree.tsx
index 61d5390..b75183a 100644
--- a/frontend/src/components/sidebar/Libraries/LibraryFileTree/LibraryFileTree.tsx
+++ b/frontend/src/components/sidebar/Libraries/LibraryFileTree/LibraryFileTree.tsx
@@ -179,6 +179,10 @@ function TreeBranch({
               }}
               type="button"
               onClick={() => onToggleFolder(directory.path)}
+              aria-expanded={open}
+              data-tree-level={
+                depth >= 3 ? "deep" : depth >= 1 ? "nested" : "root"
+              }
               title={directory.path}
             >
               <ChevronRight
diff --git a/frontend/src/components/workbench/ChatPanelToolbar.module.css b/frontend/src/components/workbench/ChatPanelToolbar.module.css
index d24b667..54fb6b4 100644
--- a/frontend/src/components/workbench/ChatPanelToolbar.module.css
+++ b/frontend/src/components/workbench/ChatPanelToolbar.module.css
@@ -5,7 +5,7 @@
   width: 100%;
   min-width: 0;
   height: 100%;
-  gap: 10px;
+  gap: 8px;
 }
 
 .activeContext {
@@ -13,20 +13,7 @@
   align-items: center;
   flex: 1;
   min-width: 0;
-  gap: 8px;
-  padding: 0 8px 0 2px;
-}
-
-.contextGlyph {
-  display: grid;
-  place-items: center;
-  width: 25px;
-  height: 25px;
-  flex: 0 0 auto;
-  border: 1px solid rgba(143, 122, 223, 0.2);
-  border-radius: 6px;
-  color: var(--app-text);
-  background: rgba(143, 122, 223, 0.08);
+  padding: 0 7px 0 1px;
 }
 
 .contextCopy {
@@ -67,18 +54,18 @@
 }
 
 .contextMeta > span:last-child {
-  max-width: 170px;
+  max-width: 140px;
 }
 
 .contextMeta svg {
   flex: 0 0 auto;
-  opacity: 0.82;
+  opacity: 0.78;
 }
 
 .contextMeta i {
   flex: 0 0 auto;
   font-style: normal;
-  opacity: 0.45;
+  opacity: 0.42;
 }
 
 .panelTabs {
@@ -86,7 +73,7 @@
   align-items: center;
   align-self: stretch;
   flex: 0 0 auto;
-  gap: 3px;
+  gap: 2px;
   padding: 4px 0;
 }
 
@@ -95,9 +82,9 @@
   align-items: center;
   justify-content: center;
   gap: 5px;
-  min-width: 62px;
+  min-width: 58px;
   height: 28px;
-  padding: 0 8px;
+  padding: 0 7px;
   border: 1px solid transparent;
   border-radius: 5px;
   color: var(--app-muted-text);
diff --git a/frontend/src/components/workbench/ChatPanelToolbar.tsx b/frontend/src/components/workbench/ChatPanelToolbar.tsx
index 4113df0..e5689b1 100644
--- a/frontend/src/components/workbench/ChatPanelToolbar.tsx
+++ b/frontend/src/components/workbench/ChatPanelToolbar.tsx
@@ -23,10 +23,6 @@ export function ChatPanelToolbar({
   return (
     <div className={styles.toolbar}>
       <div className={styles.activeContext} aria-label="Active chat context">
-        <div className={styles.contextGlyph} aria-hidden>
-          <MessageSquareText size={14} strokeWidth={2.1} />
-        </div>
-
         <div className={styles.contextCopy}>
           <strong title={selectedChat?.title ?? "No active Chat"}>
             {selectedChat?.title ?? "Select a Chat"}
diff --git a/frontend/src/components/workbench/WorkbenchShell.module.css b/frontend/src/components/workbench/WorkbenchShell.module.css
index 8db2df1..308b31c 100644
--- a/frontend/src/components/workbench/WorkbenchShell.module.css
+++ b/frontend/src/components/workbench/WorkbenchShell.module.css
@@ -1,5 +1,8 @@
 .shell {
-  --workbench-left-panel-width: clamp(252px, 21vw, 348px);
+  --workbench-left-panel-compact: clamp(210px, 15vw, 232px);
+  --workbench-left-panel-nested: clamp(222px, 17vw, 254px);
+  --workbench-left-panel-deep: clamp(236px, 19vw, 282px);
+  --workbench-left-panel-width: var(--workbench-left-panel-compact);
   --workbench-left-panel-space: var(--workbench-left-panel-width);
 
   position: fixed;
@@ -14,6 +17,20 @@
   background: var(--workspace-bg);
 }
 
+.shell:has(
+  aside[aria-label="Library workspace sidebar"]
+    button[data-tree-level="nested"][aria-expanded="true"]
+) {
+  --workbench-left-panel-width: var(--workbench-left-panel-nested);
+}
+
+.shell:has(
+  aside[aria-label="Library workspace sidebar"]
+    button[data-tree-level="deep"][aria-expanded="true"]
+) {
+  --workbench-left-panel-width: var(--workbench-left-panel-deep);
+}
+
 .leftClosed {
   --workbench-left-panel-space: 0px;
 }
@@ -46,7 +63,9 @@
 
 @media (max-width: 980px) {
   .shell {
-    --workbench-left-panel-width: clamp(244px, 30vw, 318px);
+    --workbench-left-panel-compact: clamp(204px, 25vw, 226px);
+    --workbench-left-panel-nested: clamp(218px, 28vw, 246px);
+    --workbench-left-panel-deep: clamp(232px, 31vw, 268px);
   }
 }
 
