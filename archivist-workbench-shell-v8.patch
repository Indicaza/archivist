diff --git a/frontend/src/App.tsx b/frontend/src/App.tsx
index 5631a22..2465ab2 100644
--- a/frontend/src/App.tsx
+++ b/frontend/src/App.tsx
@@ -1,5 +1,12 @@
 import { useEffect, useMemo, useState } from "react";
-import { Library as LibraryIcon, Search, Sparkles } from "lucide-react";
+import {
+  Archive,
+  Library as LibraryIcon,
+  Puzzle,
+  Search,
+  Sparkles,
+  Wrench,
+} from "lucide-react";
 import { ChatWindow } from "./components/chat/ChatWindow";
 import { AgentManagementModal } from "./components/sidebar/Agents/AgentManagementModal/AgentManagementModal";
 import { Agents } from "./components/sidebar/Agents/Agents";
@@ -9,11 +16,12 @@ import { Libraries } from "./components/sidebar/Libraries/Libraries";
 import { LibraryManagementModal } from "./components/sidebar/Libraries/LibraryManagementModal/LibraryManagementModal";
 import { Topbar } from "./components/topbar/Topbar";
 import {
-  ChatPanelToolbar,
+  ChatDockHeader,
   type ChatDockView,
-} from "./components/workbench/ChatPanelToolbar";
-import { DockPlaceholder } from "./components/workbench/DockPlaceholder";
-import { WorkbenchShell } from "./components/workbench/WorkbenchShell";
+} from "./components/workbench/ChatDock/ChatDockHeader/ChatDockHeader";
+import { DockPlaceholder } from "./components/workbench/ChatDock/DockPlaceholder/DockPlaceholder";
+import { StatusBar } from "./components/workbench/WorkbenchShell/StatusBar/StatusBar";
+import { WorkbenchShell } from "./components/workbench/WorkbenchShell/WorkbenchShell";
 import {
   addAgent,
   archiveAgent,
@@ -168,8 +176,8 @@ export function App() {
 
     return Math.round(
       Math.min(
-        202,
-        Math.max(148, 108 + Math.min(longestLabelLength, 23) * 4.1),
+        188,
+        Math.max(132, 102 + Math.min(longestLabelLength, 22) * 3.9),
       ),
     );
   }, [agents, archivedAgents, archivedChats, chatDockView, chats]);
@@ -818,6 +826,18 @@ export function App() {
               </div>
             ),
           },
+          {
+            id: "archived-libraries",
+            title: "Archived Libraries",
+            icon: <Archive size={15} strokeWidth={1.9} />,
+            content: (
+              <DockPlaceholder
+                icon={<Archive size={20} strokeWidth={1.9} />}
+                title="Archived Libraries"
+                description="Archived Library browsing will live here without crowding the active Explorer."
+              />
+            ),
+          },
           {
             id: "search",
             title: "Library Search",
@@ -831,18 +851,37 @@ export function App() {
             ),
           },
           {
-            id: "skills",
-            title: "Skills",
-            icon: <Sparkles size={15} strokeWidth={1.9} />,
+            id: "plugins",
+            title: "Plugins",
+            icon: <Puzzle size={15} strokeWidth={1.9} />,
             content: (
               <DockPlaceholder
-                icon={<Sparkles size={20} strokeWidth={1.9} />}
-                title="Skills"
-                description="Reusable tools and guided workflows will live here."
+                icon={<Puzzle size={20} strokeWidth={1.9} />}
+                title="Plugins"
+                description="Connected providers and workspace extensions will live here."
+              />
+            ),
+          },
+          {
+            id: "tools",
+            title: "Tools",
+            icon: <Wrench size={15} strokeWidth={1.9} />,
+            content: (
+              <DockPlaceholder
+                icon={<Wrench size={20} strokeWidth={1.9} />}
+                title="Tools"
+                description="Read-only tools, Skills, and observable task runs will live here."
               />
             ),
           },
         ]}
+        statusBar={
+          <StatusBar
+            libraryName={selectedLibrary?.name ?? null}
+            chatTitle={selectedChat?.title ?? null}
+            agentName={activeAgent?.name ?? null}
+          />
+        }
         artifactPanel={
           <DockPlaceholder
             icon={<Sparkles size={20} strokeWidth={1.9} />}
@@ -855,7 +894,7 @@ export function App() {
             selectedChat={selectedChat}
             onChatActivity={handleChatActivity}
             toolbar={
-              <ChatPanelToolbar
+              <ChatDockHeader
                 selectedChat={selectedChat}
                 activeAgent={activeAgent}
                 selectedLibraryName={selectedLibrary?.name ?? null}
diff --git a/frontend/src/components/chat/ChatWindow.module.css b/frontend/src/components/chat/ChatWindow.module.css
index 82244e2..8a5da92 100644
--- a/frontend/src/components/chat/ChatWindow.module.css
+++ b/frontend/src/components/chat/ChatWindow.module.css
@@ -1,5 +1,5 @@
 .chatWindow {
-  grid-column: 2;
+  grid-column: 1 / -1;
   grid-row: 1;
   z-index: 1;
   width: 100%;
@@ -7,16 +7,7 @@
   min-width: 0;
   min-height: 0;
   overflow: hidden;
-  background:
-    linear-gradient(180deg, rgba(255, 250, 240, 0.018), transparent 26%),
-    linear-gradient(
-      90deg,
-      rgba(0, 0, 0, 0.16),
-      transparent 18%,
-      transparent 82%,
-      rgba(0, 0, 0, 0.12)
-    ),
-    var(--workspace-bg);
+  background: var(--workbench-surface-bg, #181713);
   contain: layout paint style;
 }
 
@@ -27,6 +18,7 @@
   min-height: 0;
   overflow-x: hidden;
   overflow-y: auto;
+  padding-left: var(--workbench-chat-safe-left, 0px);
   overscroll-behavior: contain;
   scrollbar-gutter: stable;
 }
@@ -162,30 +154,31 @@
 }
 
 .composerPanel {
-  --chat-dock-header-height: 40px;
+  --chat-dock-header-height: 48px;
   --chat-dock-body-height: 172px;
 
-  grid-column: 2;
+  grid-column: 1 / -1;
   grid-row: 2;
   z-index: 5;
   display: grid;
   grid-template-rows:
     var(--chat-dock-header-height)
     var(--chat-dock-body-height);
-  justify-self: center;
-  width: min(calc(100% - 32px), 1120px);
+  justify-self: var(--workbench-dock-justify, center);
+  width: var(--workbench-dock-width, min(calc(100% - 32px), 1120px));
   height: calc(var(--chat-dock-header-height) + var(--chat-dock-body-height));
   min-width: 0;
   min-height: 0;
-  margin: 0 16px 12px;
+  margin: 0 var(--workbench-dock-margin-right, 16px) 10px
+    var(--workbench-dock-margin-left, 16px);
   overflow: hidden;
   border: 1px solid rgba(214, 198, 170, 0.12);
-  border-radius: 10px;
-  background: rgba(12, 11, 10, 0.96);
+  border-radius: 9px;
+  background: var(--workbench-surface-bg, #181713);
   box-shadow:
-    0 -18px 46px rgba(0, 0, 0, 0.3),
-    0 18px 40px rgba(0, 0, 0, 0.18),
-    inset 0 1px 0 rgba(255, 255, 255, 0.025);
+    0 -16px 42px rgba(0, 0, 0, 0.28),
+    0 14px 34px rgba(0, 0, 0, 0.16),
+    inset 0 1px 0 rgba(255, 255, 255, 0.022);
   contain: layout style;
 }
 
@@ -196,24 +189,7 @@
   gap: 8px;
   padding: 0 7px;
   border-bottom: 1px solid rgba(214, 198, 170, 0.09);
-  background:
-    linear-gradient(180deg, rgba(255, 255, 255, 0.025), transparent),
-    rgba(255, 255, 255, 0.01);
-}
-
-.composerTab {
-  display: inline-flex;
-  align-items: center;
-  align-self: stretch;
-  flex: 0 0 auto;
-  gap: 6px;
-  padding: 0 7px;
-  border-bottom: 2px solid var(--app-accent);
-  color: var(--app-text);
-  font-size: 9px;
-  font-weight: 750;
-  letter-spacing: 0.08em;
-  text-transform: uppercase;
+  background: rgba(255, 255, 255, 0.012);
 }
 
 .composerToolbar {
@@ -247,9 +223,7 @@
   min-width: 0;
   min-height: 0;
   overflow: hidden;
-  background:
-    linear-gradient(180deg, rgba(0, 0, 0, 0.08), transparent),
-    rgba(16, 15, 13, 0.82);
+  background: var(--workbench-surface-bg, #181713);
 }
 
 .composerInner {
@@ -259,7 +233,7 @@
   height: 100%;
   min-width: 0;
   min-height: 0;
-  background: rgba(255, 255, 255, 0.01);
+  background: var(--workbench-surface-bg, #181713);
 }
 
 .composerInner:focus-within {
@@ -384,9 +358,7 @@
   min-height: 0;
   overflow: hidden;
   border-left: 1px solid rgba(214, 198, 170, 0.09);
-  background:
-    linear-gradient(180deg, rgba(255, 255, 255, 0.018), transparent 28%),
-    rgba(10, 9, 8, 0.88);
+  background: rgba(12, 11, 10, 0.96);
 }
 
 .controlPanelHeader {
@@ -491,11 +463,6 @@
     padding-left: 4px;
   }
 
-  .composerTab {
-    padding: 0 5px;
-    font-size: 8.5px;
-  }
-
   .composerBodyWithPanel {
     grid-template-columns:
       minmax(0, 1fr)
@@ -518,7 +485,6 @@
       min(var(--chat-control-panel-width, 176px), 46vw);
   }
 
-  .composerTab span,
   .composerButton span {
     display: none;
   }
diff --git a/frontend/src/components/chat/ChatWindow.tsx b/frontend/src/components/chat/ChatWindow.tsx
index 9872387..6b89812 100644
--- a/frontend/src/components/chat/ChatWindow.tsx
+++ b/frontend/src/components/chat/ChatWindow.tsx
@@ -11,7 +11,6 @@ import {
 import {
   Code2,
   List,
-  MessageSquareText,
   Plus,
   SendHorizontal,
   Sparkles,
@@ -353,11 +352,6 @@ export function ChatWindow({
 
       <section className={styles.composerPanel} aria-label="Chat input panel">
         <header className={styles.composerHeader}>
-          <div className={styles.composerTab}>
-            <MessageSquareText size={13} strokeWidth={2.1} />
-            <span>Chat</span>
-          </div>
-
           <div className={styles.composerToolbar}>{toolbar}</div>
         </header>
 
diff --git a/frontend/src/components/sidebar/Agents/Agents.module.css b/frontend/src/components/sidebar/Agents/Agents.module.css
index 6c1c63c..2fa4d34 100644
--- a/frontend/src/components/sidebar/Agents/Agents.module.css
+++ b/frontend/src/components/sidebar/Agents/Agents.module.css
@@ -186,3 +186,55 @@
     transition: none;
   }
 }
+
+/* Shared compact-browser motion */
+.row,
+.archivedRow,
+.manageButton {
+  transform: translateX(0) scale(1);
+  transform-origin: left center;
+  transition:
+    transform 190ms var(--ease-springy),
+    color 150ms ease,
+    background-color 150ms ease,
+    border-color 150ms ease,
+    opacity 150ms ease;
+}
+
+.row:hover,
+.row:focus-visible {
+  transform: translateX(3px) scale(1.012);
+}
+
+.rowWrap:has(.row:hover) + .rowWrap .row,
+.rowWrap:has(.row:focus-visible) + .rowWrap .row,
+.rowWrap:has(+ .rowWrap .row:hover) .row,
+.rowWrap:has(+ .rowWrap .row:focus-visible) .row {
+  border-color: var(--sidebar-card-neighbor-border);
+  background: var(--sidebar-card-neighbor-bg);
+  transform: translateX(1px) scale(1.005);
+}
+
+.manageButton:hover,
+.manageButton:focus-visible {
+  transform: scale(1.08);
+}
+
+.archivedRow:hover,
+.archivedRow:focus-visible {
+  transform: translateX(3px) scale(1.008);
+}
+
+.archivedRow:hover + .archivedRow,
+.archivedRow:has(+ .archivedRow:hover) {
+  background: var(--sidebar-card-neighbor-bg);
+  transform: translateX(1px) scale(1.003);
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .row,
+  .archivedRow,
+  .manageButton {
+    transition: none;
+  }
+}
diff --git a/frontend/src/components/sidebar/Chats/Chats.module.css b/frontend/src/components/sidebar/Chats/Chats.module.css
index 6c1c63c..2fa4d34 100644
--- a/frontend/src/components/sidebar/Chats/Chats.module.css
+++ b/frontend/src/components/sidebar/Chats/Chats.module.css
@@ -186,3 +186,55 @@
     transition: none;
   }
 }
+
+/* Shared compact-browser motion */
+.row,
+.archivedRow,
+.manageButton {
+  transform: translateX(0) scale(1);
+  transform-origin: left center;
+  transition:
+    transform 190ms var(--ease-springy),
+    color 150ms ease,
+    background-color 150ms ease,
+    border-color 150ms ease,
+    opacity 150ms ease;
+}
+
+.row:hover,
+.row:focus-visible {
+  transform: translateX(3px) scale(1.012);
+}
+
+.rowWrap:has(.row:hover) + .rowWrap .row,
+.rowWrap:has(.row:focus-visible) + .rowWrap .row,
+.rowWrap:has(+ .rowWrap .row:hover) .row,
+.rowWrap:has(+ .rowWrap .row:focus-visible) .row {
+  border-color: var(--sidebar-card-neighbor-border);
+  background: var(--sidebar-card-neighbor-bg);
+  transform: translateX(1px) scale(1.005);
+}
+
+.manageButton:hover,
+.manageButton:focus-visible {
+  transform: scale(1.08);
+}
+
+.archivedRow:hover,
+.archivedRow:focus-visible {
+  transform: translateX(3px) scale(1.008);
+}
+
+.archivedRow:hover + .archivedRow,
+.archivedRow:has(+ .archivedRow:hover) {
+  background: var(--sidebar-card-neighbor-bg);
+  transform: translateX(1px) scale(1.003);
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .row,
+  .archivedRow,
+  .manageButton {
+    transition: none;
+  }
+}
diff --git a/frontend/src/components/sidebar/Libraries/Libraries.module.css b/frontend/src/components/sidebar/Libraries/Libraries.module.css
index ada4f03..88899e3 100644
--- a/frontend/src/components/sidebar/Libraries/Libraries.module.css
+++ b/frontend/src/components/sidebar/Libraries/Libraries.module.css
@@ -232,3 +232,51 @@
     max-height: 120px;
   }
 }
+
+/* Shared compact-navigation motion */
+.toolbarButton,
+.searchCloseButton,
+.archivedRow {
+  transform: translateX(0) scale(1);
+  transition:
+    transform 190ms var(--ease-springy),
+    color 150ms ease,
+    background-color 150ms ease,
+    border-color 150ms ease;
+}
+
+.toolbarButton:hover:not(:disabled),
+.toolbarButton:focus-visible:not(:disabled),
+.searchCloseButton:hover,
+.searchCloseButton:focus-visible {
+  transform: scale(1.07);
+}
+
+.toolbarButton:hover + .toolbarButton,
+.toolbarButton:focus-visible + .toolbarButton,
+.toolbarButton:has(+ .toolbarButton:hover),
+.toolbarButton:has(+ .toolbarButton:focus-visible) {
+  background: var(--sidebar-card-neighbor-bg);
+  transform: scale(1.025);
+}
+
+.archivedRow:hover,
+.archivedRow:focus-visible {
+  transform: translateX(3px) scale(1.01);
+}
+
+.archivedRow:hover + .archivedRow,
+.archivedRow:focus-visible + .archivedRow,
+.archivedRow:has(+ .archivedRow:hover),
+.archivedRow:has(+ .archivedRow:focus-visible) {
+  background: var(--sidebar-card-neighbor-bg);
+  transform: translateX(1px) scale(1.004);
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .toolbarButton,
+  .searchCloseButton,
+  .archivedRow {
+    transition: none;
+  }
+}
diff --git a/frontend/src/components/sidebar/Libraries/LibraryFileTree/LibraryFileTree.module.css b/frontend/src/components/sidebar/Libraries/LibraryFileTree/LibraryFileTree.module.css
index 1c1b5ed..b1b4d52 100644
--- a/frontend/src/components/sidebar/Libraries/LibraryFileTree/LibraryFileTree.module.css
+++ b/frontend/src/components/sidebar/Libraries/LibraryFileTree/LibraryFileTree.module.css
@@ -312,3 +312,52 @@
     transition: none;
   }
 }
+
+/* Shared Explorer motion language */
+.treeRow,
+.fileRow,
+.searchResultRow,
+.scanButton {
+  transform: translateX(0) scale(1);
+  transform-origin: left center;
+  transition:
+    transform 190ms var(--ease-springy),
+    color 150ms ease,
+    background-color 150ms ease,
+    border-color 150ms ease;
+}
+
+.treeRow:hover,
+.treeRow:focus-visible,
+.fileRow:hover,
+.searchResultRow:hover {
+  transform: translateX(3px) scale(1.008);
+}
+
+.treeViewport > div:has(> .treeRow:hover) + div > .treeRow,
+.treeViewport > div:has(> .treeRow:focus-visible) + div > .treeRow,
+.treeViewport > div:has(+ div > .treeRow:hover) > .treeRow,
+.treeViewport > div:has(+ div > .treeRow:focus-visible) > .treeRow {
+  background: var(--sidebar-card-neighbor-bg);
+  transform: translateX(1px) scale(1.003);
+}
+
+.searchResultRow:hover + .searchResultRow,
+.searchResultRow:has(+ .searchResultRow:hover) {
+  background: var(--sidebar-card-neighbor-bg);
+  transform: translateX(1px) scale(1.003);
+}
+
+.scanButton:hover:not(:disabled),
+.scanButton:focus-visible:not(:disabled) {
+  transform: scale(1.07) rotate(-8deg);
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .treeRow,
+  .fileRow,
+  .searchResultRow,
+  .scanButton {
+    transition: none;
+  }
+}
diff --git a/frontend/src/components/sidebar/Sidebar.tsx b/frontend/src/components/sidebar/Sidebar.tsx
deleted file mode 100644
index f67f1a2..0000000
--- a/frontend/src/components/sidebar/Sidebar.tsx
+++ /dev/null
@@ -1,75 +0,0 @@
-import type { ReactNode } from "react";
-import { PanelLeftClose } from "lucide-react";
-import styles from "./Sidebar.module.css";
-
-export type SidebarView = {
-  id: string;
-  title: string;
-  icon: ReactNode;
-  content: ReactNode;
-};
-
-type SidebarProps = {
-  views: SidebarView[];
-  activeViewId: string;
-  onActivateView: (viewId: string) => void;
-  onClosePanel: () => void;
-};
-
-export function Sidebar({
-  views,
-  activeViewId,
-  onActivateView,
-  onClosePanel,
-}: SidebarProps) {
-  const activeView =
-    views.find((view) => view.id === activeViewId) ?? views[0] ?? null;
-
-  if (!activeView) {
-    return null;
-  }
-
-  return (
-    <aside className={styles.sidebar} aria-label="Library workspace sidebar">
-      <header className={styles.header}>
-        <span className={styles.title}>{activeView.title}</span>
-
-        <div className={styles.headerActions}>
-          <nav className={styles.viewSwitches} aria-label="Explorer views">
-            {views.map((view) => {
-              const active = view.id === activeView.id;
-
-              return (
-                <button
-                  key={view.id}
-                  className={`${styles.headerButton} ${
-                    active ? styles.headerButtonActive : ""
-                  }`}
-                  type="button"
-                  onClick={() => onActivateView(view.id)}
-                  aria-label={view.title}
-                  aria-pressed={active}
-                  title={view.title}
-                >
-                  {view.icon}
-                </button>
-              );
-            })}
-          </nav>
-
-          <button
-            className={styles.headerButton}
-            type="button"
-            onClick={onClosePanel}
-            aria-label={`Collapse ${activeView.title}`}
-            title={`Collapse ${activeView.title}`}
-          >
-            <PanelLeftClose size={15} strokeWidth={2} />
-          </button>
-        </div>
-      </header>
-
-      <div className={styles.scrollArea}>{activeView.content}</div>
-    </aside>
-  );
-}
diff --git a/frontend/src/components/workbench/ArtifactDrawer.module.css b/frontend/src/components/workbench/ArtifactDrawer/ArtifactDrawer.module.css
similarity index 89%
rename from frontend/src/components/workbench/ArtifactDrawer.module.css
rename to frontend/src/components/workbench/ArtifactDrawer/ArtifactDrawer.module.css
index 7b02513..c1d2711 100644
--- a/frontend/src/components/workbench/ArtifactDrawer.module.css
+++ b/frontend/src/components/workbench/ArtifactDrawer/ArtifactDrawer.module.css
@@ -25,7 +25,7 @@
   border-right: 0;
   border-radius: 7px 0 0 7px;
   color: var(--app-muted-text);
-  background: rgba(16, 15, 13, 0.92);
+  background: var(--workbench-surface-bg, #181713);
   box-shadow: -6px 0 18px rgba(0, 0, 0, 0.22);
   cursor: pointer;
 }
@@ -39,7 +39,7 @@
 .toggleButton:focus-visible {
   border-color: rgba(143, 122, 223, 0.32);
   color: var(--app-text);
-  background: rgba(28, 26, 23, 0.97);
+  background: rgba(31, 29, 24, 0.98);
   outline: none;
 }
 
@@ -52,9 +52,7 @@
   overflow: hidden;
   border: 1px solid var(--sidebar-border);
   border-radius: 0 9px 9px 9px;
-  background:
-    linear-gradient(180deg, rgba(255, 250, 240, 0.018), transparent 24%),
-    rgba(16, 15, 13, 0.96);
+  background: var(--workbench-surface-bg, #181713);
   box-shadow: 0 20px 50px rgba(0, 0, 0, 0.52);
   contain: layout paint style;
 }
diff --git a/frontend/src/components/workbench/ArtifactDrawer.tsx b/frontend/src/components/workbench/ArtifactDrawer/ArtifactDrawer.tsx
similarity index 100%
rename from frontend/src/components/workbench/ArtifactDrawer.tsx
rename to frontend/src/components/workbench/ArtifactDrawer/ArtifactDrawer.tsx
diff --git a/frontend/src/components/workbench/ChatDock/ChatDockHeader/ChatDockHeader.module.css b/frontend/src/components/workbench/ChatDock/ChatDockHeader/ChatDockHeader.module.css
new file mode 100644
index 0000000..13018fa
--- /dev/null
+++ b/frontend/src/components/workbench/ChatDock/ChatDockHeader/ChatDockHeader.module.css
@@ -0,0 +1,181 @@
+.header {
+  display: flex;
+  align-items: stretch;
+  justify-content: space-between;
+  width: 100%;
+  min-width: 0;
+  height: 100%;
+  gap: 8px;
+}
+
+.identity {
+  display: grid;
+  align-content: center;
+  flex: 1;
+  min-width: 0;
+  gap: 1px;
+  padding: 3px 7px 3px 1px;
+}
+
+.eyebrow {
+  color: var(--app-muted-text);
+  font-size: 7.5px;
+  font-weight: 700;
+  letter-spacing: 0.11em;
+  line-height: 1;
+  text-transform: uppercase;
+  opacity: 0.72;
+}
+
+.identity > strong {
+  min-width: 0;
+  overflow: hidden;
+  color: var(--app-text);
+  font-size: 11px;
+  font-weight: 680;
+  line-height: 1.15;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.contextChips {
+  display: flex;
+  align-items: center;
+  min-width: 0;
+  gap: 4px;
+  overflow: hidden;
+}
+
+.contextChips > span {
+  display: inline-flex;
+  align-items: center;
+  min-width: 0;
+  max-width: 170px;
+  height: 15px;
+  gap: 3px;
+  padding: 0 5px;
+  overflow: hidden;
+  border: 1px solid rgba(214, 198, 170, 0.07);
+  border-radius: var(--r-pill);
+  color: var(--app-muted-text);
+  background: rgba(255, 255, 255, 0.018);
+  font-size: 7.8px;
+  white-space: nowrap;
+}
+
+.contextChips svg {
+  flex: 0 0 auto;
+  opacity: 0.72;
+}
+
+.contextChips b {
+  flex: 0 0 auto;
+  font-weight: 700;
+  opacity: 0.7;
+}
+
+.contextChips i {
+  min-width: 0;
+  overflow: hidden;
+  color: var(--app-text);
+  font-style: normal;
+  text-overflow: ellipsis;
+}
+
+.actions {
+  display: flex;
+  align-items: center;
+  align-self: stretch;
+  flex: 0 0 auto;
+  gap: 2px;
+  padding: 4px 0;
+}
+
+.modeButton,
+.tab {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  height: 29px;
+  padding: 0 7px;
+  border: 1px solid transparent;
+  border-radius: 5px;
+  color: var(--app-muted-text);
+  background: transparent;
+  font: inherit;
+  font-size: 9px;
+  font-weight: 650;
+  cursor: pointer;
+  transform: translateY(0) scale(1);
+  transition:
+    transform 190ms var(--ease-springy),
+    color 150ms ease,
+    background-color 150ms ease,
+    border-color 150ms ease,
+    box-shadow 180ms ease;
+}
+
+.modeButton {
+  width: 29px;
+  padding: 0;
+}
+
+.tab {
+  min-width: 56px;
+  gap: 5px;
+}
+
+.modeButton:hover,
+.modeButton:focus-visible,
+.tab:hover,
+.tab:focus-visible {
+  border-color: var(--sidebar-card-hover-border);
+  color: var(--app-text);
+  background: var(--sidebar-card-hover-bg);
+  box-shadow: var(--sidebar-card-hover-shadow);
+  outline: none;
+  transform: translateY(-1px) scale(1.035);
+}
+
+.actions:has(.modeButton:hover) .tab:first-of-type,
+.actions:has(.modeButton:focus-visible) .tab:first-of-type,
+.actions:has(.tab:first-of-type:hover) .modeButton,
+.actions:has(.tab:first-of-type:focus-visible) .modeButton,
+.actions:has(.tab:first-of-type:hover) .tab:last-of-type,
+.actions:has(.tab:first-of-type:focus-visible) .tab:last-of-type,
+.actions:has(.tab:last-of-type:hover) .tab:first-of-type,
+.actions:has(.tab:last-of-type:focus-visible) .tab:first-of-type {
+  border-color: var(--sidebar-card-neighbor-border);
+  background: var(--sidebar-card-neighbor-bg);
+  transform: scale(1.015);
+}
+
+.activeTab {
+  border-color: rgba(143, 122, 223, 0.25);
+  color: var(--app-text);
+  background: rgba(143, 122, 223, 0.11);
+  box-shadow: inset 0 -1px 0 rgba(143, 122, 223, 0.48);
+}
+
+@media (max-width: 700px) {
+  .contextChips > span:last-child {
+    display: none;
+  }
+
+  .tab {
+    min-width: 29px;
+    width: 29px;
+    padding: 0;
+  }
+
+  .tab span {
+    display: none;
+  }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .modeButton,
+  .tab {
+    transition: none;
+  }
+}
diff --git a/frontend/src/components/workbench/ChatDock/ChatDockHeader/ChatDockHeader.tsx b/frontend/src/components/workbench/ChatDock/ChatDockHeader/ChatDockHeader.tsx
new file mode 100644
index 0000000..7c361bc
--- /dev/null
+++ b/frontend/src/components/workbench/ChatDock/ChatDockHeader/ChatDockHeader.tsx
@@ -0,0 +1,105 @@
+import {
+  Bot,
+  FolderOpen,
+  Maximize2,
+  MessageSquareText,
+  Minimize2,
+} from "lucide-react";
+import type { Agent } from "../../../../domains/agent/agent.types";
+import type { Chat } from "../../../../domains/chat/chat.types";
+import { useWorkbenchLayout } from "../../WorkbenchShell/WorkbenchLayoutContext";
+import styles from "./ChatDockHeader.module.css";
+
+export type ChatDockView = "chats" | "agents" | null;
+
+type ChatDockHeaderProps = {
+  selectedChat: Chat | null;
+  activeAgent: Agent | null;
+  selectedLibraryName: string | null;
+  activeView: ChatDockView;
+  onToggleView: (view: Exclude<ChatDockView, null>) => void;
+};
+
+export function ChatDockHeader({
+  selectedChat,
+  activeAgent,
+  selectedLibraryName,
+  activeView,
+  onToggleView,
+}: ChatDockHeaderProps) {
+  const { dockMode, effectiveDockMode, leftOpen, toggleDockMode } =
+    useWorkbenchLayout();
+
+  const dockModeTitle =
+    dockMode === "attached" && leftOpen
+      ? "Center Chat Dock"
+      : "Attach Chat Dock to Explorer";
+
+  return (
+    <div className={styles.header}>
+      <div className={styles.identity} aria-label="Active chat context">
+        <span className={styles.eyebrow}>Current Chat</span>
+
+        <strong title={selectedChat?.title ?? "No active Chat"}>
+          {selectedChat?.title ?? "Select a Chat"}
+        </strong>
+
+        <div className={styles.contextChips}>
+          <span title={activeAgent?.name ?? "No active Agent"}>
+            <Bot size={10} strokeWidth={2} />
+            <b>Agent</b>
+            <i>{activeAgent?.name ?? "None"}</i>
+          </span>
+
+          <span title={selectedLibraryName ?? "Standalone workspace"}>
+            <FolderOpen size={10} strokeWidth={2} />
+            <b>Library</b>
+            <i>{selectedLibraryName ?? "Standalone"}</i>
+          </span>
+        </div>
+      </div>
+
+      <nav className={styles.actions} aria-label="Chat dock controls">
+        <button
+          className={styles.modeButton}
+          type="button"
+          onClick={toggleDockMode}
+          aria-label={dockModeTitle}
+          title={dockModeTitle}
+        >
+          {effectiveDockMode === "attached" ? (
+            <Minimize2 size={13} strokeWidth={2.1} />
+          ) : (
+            <Maximize2 size={13} strokeWidth={2.1} />
+          )}
+        </button>
+
+        <button
+          className={`${styles.tab} ${
+            activeView === "chats" ? styles.activeTab : ""
+          }`}
+          type="button"
+          onClick={() => onToggleView("chats")}
+          aria-expanded={activeView === "chats"}
+          title="Browse Chats"
+        >
+          <MessageSquareText size={13} strokeWidth={2.1} />
+          <span>Chats</span>
+        </button>
+
+        <button
+          className={`${styles.tab} ${
+            activeView === "agents" ? styles.activeTab : ""
+          }`}
+          type="button"
+          onClick={() => onToggleView("agents")}
+          aria-expanded={activeView === "agents"}
+          title="Browse Agents"
+        >
+          <Bot size={13} strokeWidth={2.1} />
+          <span>Agents</span>
+        </button>
+      </nav>
+    </div>
+  );
+}
diff --git a/frontend/src/components/workbench/DockPlaceholder.module.css b/frontend/src/components/workbench/ChatDock/DockPlaceholder/DockPlaceholder.module.css
similarity index 100%
rename from frontend/src/components/workbench/DockPlaceholder.module.css
rename to frontend/src/components/workbench/ChatDock/DockPlaceholder/DockPlaceholder.module.css
diff --git a/frontend/src/components/workbench/DockPlaceholder.tsx b/frontend/src/components/workbench/ChatDock/DockPlaceholder/DockPlaceholder.tsx
similarity index 100%
rename from frontend/src/components/workbench/DockPlaceholder.tsx
rename to frontend/src/components/workbench/ChatDock/DockPlaceholder/DockPlaceholder.tsx
diff --git a/frontend/src/components/workbench/ChatPanelToolbar.module.css b/frontend/src/components/workbench/ChatPanelToolbar.module.css
deleted file mode 100644
index 54fb6b4..0000000
--- a/frontend/src/components/workbench/ChatPanelToolbar.module.css
+++ /dev/null
@@ -1,128 +0,0 @@
-.toolbar {
-  display: flex;
-  align-items: stretch;
-  justify-content: space-between;
-  width: 100%;
-  min-width: 0;
-  height: 100%;
-  gap: 8px;
-}
-
-.activeContext {
-  display: flex;
-  align-items: center;
-  flex: 1;
-  min-width: 0;
-  padding: 0 7px 0 1px;
-}
-
-.contextCopy {
-  display: grid;
-  min-width: 0;
-  gap: 1px;
-}
-
-.contextCopy strong {
-  min-width: 0;
-  overflow: hidden;
-  color: var(--app-text);
-  font-size: 11px;
-  font-weight: 650;
-  line-height: 1.15;
-  text-overflow: ellipsis;
-  white-space: nowrap;
-}
-
-.contextMeta {
-  display: flex;
-  align-items: center;
-  min-width: 0;
-  gap: 5px;
-  color: var(--app-muted-text);
-  font-size: 8.5px;
-  line-height: 1.15;
-}
-
-.contextMeta > span {
-  display: inline-flex;
-  align-items: center;
-  min-width: 0;
-  gap: 3px;
-  overflow: hidden;
-  text-overflow: ellipsis;
-  white-space: nowrap;
-}
-
-.contextMeta > span:last-child {
-  max-width: 140px;
-}
-
-.contextMeta svg {
-  flex: 0 0 auto;
-  opacity: 0.78;
-}
-
-.contextMeta i {
-  flex: 0 0 auto;
-  font-style: normal;
-  opacity: 0.42;
-}
-
-.panelTabs {
-  display: flex;
-  align-items: center;
-  align-self: stretch;
-  flex: 0 0 auto;
-  gap: 2px;
-  padding: 4px 0;
-}
-
-.panelTab {
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  gap: 5px;
-  min-width: 58px;
-  height: 28px;
-  padding: 0 7px;
-  border: 1px solid transparent;
-  border-radius: 5px;
-  color: var(--app-muted-text);
-  background: transparent;
-  font: inherit;
-  font-size: 9.5px;
-  font-weight: 650;
-  cursor: pointer;
-}
-
-.panelTab:hover,
-.panelTab:focus-visible {
-  border-color: rgba(214, 198, 170, 0.09);
-  color: var(--app-text);
-  background: rgba(255, 255, 255, 0.04);
-  outline: none;
-}
-
-.panelTabActive {
-  border-color: rgba(143, 122, 223, 0.25);
-  color: var(--app-text);
-  background: rgba(143, 122, 223, 0.11);
-  box-shadow: inset 0 -1px 0 rgba(143, 122, 223, 0.48);
-}
-
-@media (max-width: 680px) {
-  .contextMeta > span:last-child,
-  .contextMeta i {
-    display: none;
-  }
-
-  .panelTab {
-    min-width: 30px;
-    width: 30px;
-    padding: 0;
-  }
-
-  .panelTab span {
-    display: none;
-  }
-}
diff --git a/frontend/src/components/workbench/ChatPanelToolbar.tsx b/frontend/src/components/workbench/ChatPanelToolbar.tsx
deleted file mode 100644
index e5689b1..0000000
--- a/frontend/src/components/workbench/ChatPanelToolbar.tsx
+++ /dev/null
@@ -1,76 +0,0 @@
-import { Bot, FolderOpen, MessageSquareText } from "lucide-react";
-import type { Agent } from "../../domains/agent/agent.types";
-import type { Chat } from "../../domains/chat/chat.types";
-import styles from "./ChatPanelToolbar.module.css";
-
-export type ChatDockView = "chats" | "agents" | null;
-
-type ChatPanelToolbarProps = {
-  selectedChat: Chat | null;
-  activeAgent: Agent | null;
-  selectedLibraryName: string | null;
-  activeView: ChatDockView;
-  onToggleView: (view: Exclude<ChatDockView, null>) => void;
-};
-
-export function ChatPanelToolbar({
-  selectedChat,
-  activeAgent,
-  selectedLibraryName,
-  activeView,
-  onToggleView,
-}: ChatPanelToolbarProps) {
-  return (
-    <div className={styles.toolbar}>
-      <div className={styles.activeContext} aria-label="Active chat context">
-        <div className={styles.contextCopy}>
-          <strong title={selectedChat?.title ?? "No active Chat"}>
-            {selectedChat?.title ?? "Select a Chat"}
-          </strong>
-
-          <span className={styles.contextMeta}>
-            <span title={activeAgent?.name ?? "No active Agent"}>
-              <Bot size={10} strokeWidth={2} />
-              {activeAgent?.name ?? "No Agent"}
-            </span>
-
-            <i aria-hidden>·</i>
-
-            <span title={selectedLibraryName ?? "Standalone workspace"}>
-              <FolderOpen size={10} strokeWidth={2} />
-              {selectedLibraryName ?? "Standalone"}
-            </span>
-          </span>
-        </div>
-      </div>
-
-      <nav className={styles.panelTabs} aria-label="Chat dock browsers">
-        <button
-          className={`${styles.panelTab} ${
-            activeView === "chats" ? styles.panelTabActive : ""
-          }`}
-          type="button"
-          onClick={() => onToggleView("chats")}
-          aria-expanded={activeView === "chats"}
-          title="Browse Chats"
-        >
-          <MessageSquareText size={13} strokeWidth={2.1} />
-          <span>Chats</span>
-        </button>
-
-        <button
-          className={`${styles.panelTab} ${
-            activeView === "agents" ? styles.panelTabActive : ""
-          }`}
-          type="button"
-          onClick={() => onToggleView("agents")}
-          aria-expanded={activeView === "agents"}
-          title="Browse Agents"
-        >
-          <Bot size={13} strokeWidth={2.1} />
-          <span>Agents</span>
-        </button>
-      </nav>
-    </div>
-  );
-}
diff --git a/frontend/src/components/workbench/WorkbenchCanvas.module.css b/frontend/src/components/workbench/WorkbenchCanvas/WorkbenchCanvas.module.css
similarity index 100%
rename from frontend/src/components/workbench/WorkbenchCanvas.module.css
rename to frontend/src/components/workbench/WorkbenchCanvas/WorkbenchCanvas.module.css
diff --git a/frontend/src/components/workbench/WorkbenchCanvas.tsx b/frontend/src/components/workbench/WorkbenchCanvas/WorkbenchCanvas.tsx
similarity index 90%
rename from frontend/src/components/workbench/WorkbenchCanvas.tsx
rename to frontend/src/components/workbench/WorkbenchCanvas/WorkbenchCanvas.tsx
index 842d2f1..d9b75a5 100644
--- a/frontend/src/components/workbench/WorkbenchCanvas.tsx
+++ b/frontend/src/components/workbench/WorkbenchCanvas/WorkbenchCanvas.tsx
@@ -1,7 +1,7 @@
 import { Bot, FolderOpen, MessageSquareText, Sparkles } from "lucide-react";
-import type { Agent } from "../../domains/agent/agent.types";
-import type { Chat } from "../../domains/chat/chat.types";
-import type { LibraryListItem } from "../../domains/library/library.types";
+import type { Agent } from "../../../domains/agent/agent.types";
+import type { Chat } from "../../../domains/chat/chat.types";
+import type { LibraryListItem } from "../../../domains/library/library.types";
 import styles from "./WorkbenchCanvas.module.css";
 
 type WorkbenchCanvasProps = {
diff --git a/frontend/src/components/workbench/WorkbenchShell.module.css b/frontend/src/components/workbench/WorkbenchShell.module.css
deleted file mode 100644
index 308b31c..0000000
--- a/frontend/src/components/workbench/WorkbenchShell.module.css
+++ /dev/null
@@ -1,76 +0,0 @@
-.shell {
-  --workbench-left-panel-compact: clamp(210px, 15vw, 232px);
-  --workbench-left-panel-nested: clamp(222px, 17vw, 254px);
-  --workbench-left-panel-deep: clamp(236px, 19vw, 282px);
-  --workbench-left-panel-width: var(--workbench-left-panel-compact);
-  --workbench-left-panel-space: var(--workbench-left-panel-width);
-
-  position: fixed;
-  inset: var(--topbar-height) 0 0;
-  display: grid;
-  grid-template-columns: var(--workbench-left-panel-space) minmax(0, 1fr);
-  grid-template-rows: minmax(0, 1fr) auto;
-  min-width: 0;
-  min-height: 0;
-  overflow: hidden;
-  color: var(--app-text);
-  background: var(--workspace-bg);
-}
-
-.shell:has(
-  aside[aria-label="Library workspace sidebar"]
-    button[data-tree-level="nested"][aria-expanded="true"]
-) {
-  --workbench-left-panel-width: var(--workbench-left-panel-nested);
-}
-
-.shell:has(
-  aside[aria-label="Library workspace sidebar"]
-    button[data-tree-level="deep"][aria-expanded="true"]
-) {
-  --workbench-left-panel-width: var(--workbench-left-panel-deep);
-}
-
-.leftClosed {
-  --workbench-left-panel-space: 0px;
-}
-
-.restoreExplorerButton {
-  position: absolute;
-  z-index: 9;
-  top: 9px;
-  left: 9px;
-  display: grid;
-  place-items: center;
-  width: 30px;
-  height: 30px;
-  padding: 0;
-  border: 1px solid rgba(214, 198, 170, 0.12);
-  border-radius: 7px;
-  color: var(--app-muted-text);
-  background: rgba(16, 15, 13, 0.9);
-  box-shadow: var(--shadow-soft);
-  cursor: pointer;
-}
-
-.restoreExplorerButton:hover,
-.restoreExplorerButton:focus-visible {
-  border-color: rgba(143, 122, 223, 0.28);
-  color: var(--app-text);
-  background: rgba(28, 26, 23, 0.96);
-  outline: none;
-}
-
-@media (max-width: 980px) {
-  .shell {
-    --workbench-left-panel-compact: clamp(204px, 25vw, 226px);
-    --workbench-left-panel-nested: clamp(218px, 28vw, 246px);
-    --workbench-left-panel-deep: clamp(232px, 31vw, 268px);
-  }
-}
-
-@media (max-width: 800px) {
-  .shell {
-    --workbench-left-panel-space: 0px;
-  }
-}
diff --git a/frontend/src/components/workbench/WorkbenchShell.tsx b/frontend/src/components/workbench/WorkbenchShell.tsx
deleted file mode 100644
index f179dff..0000000
--- a/frontend/src/components/workbench/WorkbenchShell.tsx
+++ /dev/null
@@ -1,98 +0,0 @@
-import { type ReactNode, useEffect, useRef, useState } from "react";
-import { PanelLeftOpen } from "lucide-react";
-import { Sidebar, type SidebarView } from "../sidebar/Sidebar";
-import { ArtifactDrawer } from "./ArtifactDrawer";
-import styles from "./WorkbenchShell.module.css";
-
-export type WorkbenchDockView = SidebarView;
-
-type WorkbenchShellProps = {
-  leftViews: WorkbenchDockView[];
-  workspace: ReactNode;
-  artifactPanel: ReactNode;
-};
-
-type StoredLayout = {
-  activeLeftViewId: string;
-  leftOpen: boolean;
-};
-
-const STORAGE_KEY = "archivist.workbench.layout.v5";
-
-function readStoredLayout(): StoredLayout | null {
-  try {
-    const rawValue = window.localStorage.getItem(STORAGE_KEY);
-
-    if (!rawValue) {
-      return null;
-    }
-
-    return JSON.parse(rawValue) as StoredLayout;
-  } catch {
-    return null;
-  }
-}
-
-export function WorkbenchShell({
-  leftViews,
-  workspace,
-  artifactPanel,
-}: WorkbenchShellProps) {
-  const storedLayout = useRef(readStoredLayout());
-
-  const [activeLeftViewId, setActiveLeftViewId] = useState(
-    storedLayout.current?.activeLeftViewId ?? leftViews[0]?.id ?? "",
-  );
-  const [leftOpen, setLeftOpen] = useState(
-    storedLayout.current?.leftOpen ?? true,
-  );
-
-  useEffect(() => {
-    const nextLayout: StoredLayout = {
-      activeLeftViewId,
-      leftOpen,
-    };
-
-    window.localStorage.setItem(STORAGE_KEY, JSON.stringify(nextLayout));
-  }, [activeLeftViewId, leftOpen]);
-
-  function activateLeftView(viewId: string) {
-    if (viewId === activeLeftViewId && leftOpen) {
-      return;
-    }
-
-    setActiveLeftViewId(viewId);
-    setLeftOpen(true);
-  }
-
-  return (
-    <div
-      className={`${styles.shell} ${
-        leftOpen ? styles.leftOpen : styles.leftClosed
-      }`}
-    >
-      {leftOpen ? (
-        <Sidebar
-          views={leftViews}
-          activeViewId={activeLeftViewId}
-          onActivateView={activateLeftView}
-          onClosePanel={() => setLeftOpen(false)}
-        />
-      ) : (
-        <button
-          className={styles.restoreExplorerButton}
-          type="button"
-          onClick={() => setLeftOpen(true)}
-          aria-label="Open Library Explorer"
-          title="Open Library Explorer"
-        >
-          <PanelLeftOpen size={17} strokeWidth={2} />
-        </button>
-      )}
-
-      {workspace}
-
-      <ArtifactDrawer>{artifactPanel}</ArtifactDrawer>
-    </div>
-  );
-}
diff --git a/frontend/src/components/workbench/WorkbenchShell/ActivityRail/ActivityRail.module.css b/frontend/src/components/workbench/WorkbenchShell/ActivityRail/ActivityRail.module.css
new file mode 100644
index 0000000..81565c1
--- /dev/null
+++ b/frontend/src/components/workbench/WorkbenchShell/ActivityRail/ActivityRail.module.css
@@ -0,0 +1,115 @@
+.rail {
+  grid-column: 1;
+  grid-row: 1 / 3;
+  z-index: 12;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  width: var(--workbench-activity-rail-width);
+  min-height: 0;
+  padding: 6px 4px;
+  border-right: 1px solid rgba(214, 198, 170, 0.09);
+  background: rgba(12, 11, 10, 0.98);
+  box-shadow: 6px 0 20px rgba(0, 0, 0, 0.16);
+}
+
+.list {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  width: 100%;
+  gap: 3px;
+  margin: 0;
+  padding: 0;
+  list-style: none;
+}
+
+.item {
+  width: 100%;
+}
+
+.button {
+  position: relative;
+  display: grid;
+  place-items: center;
+  width: 100%;
+  height: 31px;
+  padding: 0;
+  border: 1px solid transparent;
+  border-radius: 6px;
+  color: var(--app-muted-text);
+  background: transparent;
+  cursor: pointer;
+  transform: translateX(0) scale(1);
+  transition:
+    transform 210ms var(--ease-springy),
+    color 150ms ease,
+    background-color 150ms ease,
+    border-color 150ms ease,
+    box-shadow 180ms ease;
+}
+
+.button svg {
+  width: 16px;
+  height: 16px;
+  transition: transform 210ms var(--ease-springy);
+}
+
+.button:hover,
+.button:focus-visible {
+  border-color: var(--sidebar-card-hover-border);
+  color: var(--app-text);
+  background: var(--sidebar-card-hover-bg);
+  box-shadow: var(--sidebar-card-hover-shadow);
+  outline: none;
+  transform: translateX(2px) scale(1.06);
+}
+
+.button:hover svg,
+.button:focus-visible svg {
+  transform: scale(1.08) rotate(-2deg);
+}
+
+.item:has(.button:hover) + .item .button,
+.item:has(.button:focus-visible) + .item .button,
+.item:has(+ .item .button:hover) .button,
+.item:has(+ .item .button:focus-visible) .button {
+  border-color: var(--sidebar-card-neighbor-border);
+  background: var(--sidebar-card-neighbor-bg);
+  transform: translateX(1px) scale(1.025);
+}
+
+.active {
+  border-color: rgba(143, 122, 223, 0.24);
+  color: var(--app-text);
+  background: rgba(143, 122, 223, 0.12);
+  box-shadow: inset 0 0 0 1px rgba(143, 122, 223, 0.05);
+}
+
+.active::before {
+  content: "";
+  position: absolute;
+  top: 6px;
+  bottom: 6px;
+  left: -4px;
+  width: 2px;
+  border-radius: var(--r-pill);
+  background: var(--app-accent);
+}
+
+@media (max-height: 620px) {
+  .rail {
+    padding-top: 4px;
+  }
+
+  .button {
+    height: 28px;
+  }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .button,
+  .button svg {
+    transition: none;
+  }
+}
diff --git a/frontend/src/components/workbench/WorkbenchShell/ActivityRail/ActivityRail.tsx b/frontend/src/components/workbench/WorkbenchShell/ActivityRail/ActivityRail.tsx
new file mode 100644
index 0000000..af59587
--- /dev/null
+++ b/frontend/src/components/workbench/WorkbenchShell/ActivityRail/ActivityRail.tsx
@@ -0,0 +1,41 @@
+import type { WorkbenchDockView } from "../WorkbenchShell";
+import styles from "./ActivityRail.module.css";
+
+type ActivityRailProps = {
+  views: WorkbenchDockView[];
+  activeViewId: string;
+  panelOpen: boolean;
+  onActivateView: (viewId: string) => void;
+};
+
+export function ActivityRail({
+  views,
+  activeViewId,
+  panelOpen,
+  onActivateView,
+}: ActivityRailProps) {
+  return (
+    <nav className={styles.rail} aria-label="Workspace navigation">
+      <ul className={styles.list}>
+        {views.map((view) => {
+          const active = panelOpen && view.id === activeViewId;
+
+          return (
+            <li key={view.id} className={styles.item}>
+              <button
+                className={`${styles.button} ${active ? styles.active : ""}`}
+                type="button"
+                onClick={() => onActivateView(view.id)}
+                aria-label={view.title}
+                aria-pressed={active}
+                title={view.title}
+              >
+                {view.icon}
+              </button>
+            </li>
+          );
+        })}
+      </ul>
+    </nav>
+  );
+}
diff --git a/frontend/src/components/sidebar/Sidebar.module.css b/frontend/src/components/workbench/WorkbenchShell/ExplorerDock/ExplorerDock.module.css
similarity index 54%
rename from frontend/src/components/sidebar/Sidebar.module.css
rename to frontend/src/components/workbench/WorkbenchShell/ExplorerDock/ExplorerDock.module.css
index b10612b..b559bef 100644
--- a/frontend/src/components/sidebar/Sidebar.module.css
+++ b/frontend/src/components/workbench/WorkbenchShell/ExplorerDock/ExplorerDock.module.css
@@ -1,7 +1,9 @@
-.sidebar {
-  grid-column: 1;
-  grid-row: 1 / 3;
-  z-index: 6;
+.dock {
+  position: absolute;
+  z-index: 10;
+  top: 0;
+  bottom: var(--workbench-statusbar-height);
+  left: var(--workbench-activity-rail-width);
   display: grid;
   grid-template-rows: 34px minmax(0, 1fr);
   width: var(--workbench-left-panel-width);
@@ -10,10 +12,8 @@
   overflow: hidden;
   border-right: 1px solid var(--sidebar-border);
   color: var(--app-text);
-  background:
-    linear-gradient(180deg, rgba(255, 250, 240, 0.018), transparent 24%),
-    var(--sidebar-bg);
-  box-shadow: var(--shadow-panel);
+  background: var(--workbench-surface-bg);
+  box-shadow: 12px 0 34px rgba(0, 0, 0, 0.24);
   contain: layout paint style;
 }
 
@@ -23,10 +23,10 @@
   justify-content: space-between;
   min-width: 0;
   min-height: 34px;
-  gap: 7px;
+  gap: 8px;
   padding: 4px 5px 4px 9px;
   border-bottom: 1px solid rgba(214, 198, 170, 0.09);
-  background: rgba(255, 255, 255, 0.014);
+  background: rgba(255, 255, 255, 0.012);
 }
 
 .title {
@@ -41,89 +41,55 @@
   white-space: nowrap;
 }
 
-.headerActions,
-.viewSwitches {
-  display: flex;
-  align-items: center;
-  flex: 0 0 auto;
-  gap: 2px;
-}
-
-.headerButton {
-  position: relative;
+.closeButton {
   display: grid;
   place-items: center;
+  flex: 0 0 auto;
   width: 25px;
   height: 25px;
   padding: 0;
-  border: 0;
+  border: 1px solid transparent;
   border-radius: 4px;
   color: var(--app-muted-text);
   background: transparent;
   cursor: pointer;
+  transition:
+    transform 190ms var(--ease-springy),
+    color 150ms ease,
+    background-color 150ms ease,
+    border-color 150ms ease;
 }
 
-.headerButton svg {
-  width: 14px;
-  height: 14px;
-}
-
-.headerButton:hover,
-.headerButton:focus-visible {
+.closeButton:hover,
+.closeButton:focus-visible {
+  border-color: var(--sidebar-card-hover-border);
   color: var(--app-text);
-  background: var(--sidebar-reveal-hover);
+  background: var(--sidebar-card-hover-bg);
   outline: none;
+  transform: scale(1.07);
 }
 
-.headerButtonActive {
-  color: var(--app-text);
-  background: rgba(143, 122, 223, 0.1);
-}
-
-.headerButtonActive::after {
-  content: "";
-  position: absolute;
-  right: 5px;
-  bottom: 2px;
-  left: 5px;
-  height: 1px;
-  border-radius: var(--r-pill);
-  background: var(--app-accent);
-}
-
-.divider {
-  width: 1px;
-  height: 17px;
-  margin: 0 2px;
-  background: rgba(214, 198, 170, 0.1);
-}
-
-.scrollArea {
+.content {
   display: flex;
   flex-direction: column;
   min-width: 0;
   min-height: 0;
   overflow: hidden;
-  overscroll-behavior: contain;
 }
 
-.scrollArea > * {
+.content > * {
   min-height: 0;
 }
 
 @media (max-width: 800px) {
-  .sidebar {
-    position: absolute;
-    top: 0;
-    bottom: 0;
-    left: 0;
-    width: min(var(--workbench-left-panel-width), calc(100vw - 42px));
-    box-shadow: 14px 0 42px rgba(0, 0, 0, 0.66);
+  .dock {
+    width: min(var(--workbench-left-panel-width), calc(100vw - 48px));
+    box-shadow: 18px 0 44px rgba(0, 0, 0, 0.62);
   }
 }
 
 @media (max-height: 620px) {
-  .sidebar {
+  .dock {
     grid-template-rows: 30px minmax(0, 1fr);
   }
 
@@ -133,3 +99,9 @@
     padding-bottom: 2px;
   }
 }
+
+@media (prefers-reduced-motion: reduce) {
+  .closeButton {
+    transition: none;
+  }
+}
diff --git a/frontend/src/components/workbench/WorkbenchShell/ExplorerDock/ExplorerDock.tsx b/frontend/src/components/workbench/WorkbenchShell/ExplorerDock/ExplorerDock.tsx
new file mode 100644
index 0000000..5fa9389
--- /dev/null
+++ b/frontend/src/components/workbench/WorkbenchShell/ExplorerDock/ExplorerDock.tsx
@@ -0,0 +1,46 @@
+import { PanelLeftClose } from "lucide-react";
+import type { WorkbenchDockView } from "../WorkbenchShell";
+import styles from "./ExplorerDock.module.css";
+
+type ExplorerDockProps = {
+  views: WorkbenchDockView[];
+  activeViewId: string;
+  onClose: () => void;
+};
+
+export function ExplorerDock({
+  views,
+  activeViewId,
+  onClose,
+}: ExplorerDockProps) {
+  const activeView =
+    views.find((view) => view.id === activeViewId) ?? views[0] ?? null;
+
+  if (!activeView) {
+    return null;
+  }
+
+  return (
+    <aside
+      className={styles.dock}
+      aria-label="Library workspace sidebar"
+      data-workbench-explorer
+    >
+      <header className={styles.header}>
+        <span className={styles.title}>{activeView.title}</span>
+
+        <button
+          className={styles.closeButton}
+          type="button"
+          onClick={onClose}
+          aria-label={`Collapse ${activeView.title}`}
+          title={`Collapse ${activeView.title}`}
+        >
+          <PanelLeftClose size={15} strokeWidth={2} />
+        </button>
+      </header>
+
+      <div className={styles.content}>{activeView.content}</div>
+    </aside>
+  );
+}
diff --git a/frontend/src/components/workbench/WorkbenchShell/StatusBar/StatusBar.module.css b/frontend/src/components/workbench/WorkbenchShell/StatusBar/StatusBar.module.css
new file mode 100644
index 0000000..b7b1c2c
--- /dev/null
+++ b/frontend/src/components/workbench/WorkbenchShell/StatusBar/StatusBar.module.css
@@ -0,0 +1,73 @@
+.statusBar {
+  grid-column: 1 / -1;
+  grid-row: 3;
+  z-index: 20;
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  width: 100%;
+  height: var(--workbench-statusbar-height);
+  min-width: 0;
+  gap: 12px;
+  padding: 0 8px;
+  border-top: 1px solid rgba(214, 198, 170, 0.1);
+  color: var(--app-muted-text);
+  background: rgba(10, 9, 8, 0.98);
+  font-size: 8.5px;
+  box-shadow: 0 -6px 18px rgba(0, 0, 0, 0.14);
+}
+
+.group,
+.contextGroup,
+.group > span {
+  display: flex;
+  align-items: center;
+  min-width: 0;
+}
+
+.group {
+  flex: 0 0 auto;
+  gap: 9px;
+}
+
+.group > span {
+  gap: 4px;
+  white-space: nowrap;
+}
+
+.contextGroup {
+  justify-content: flex-end;
+  flex: 1;
+  gap: 10px;
+  overflow: hidden;
+}
+
+.contextGroup > span {
+  max-width: 150px;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+
+.readyDot {
+  width: 6px;
+  height: 6px;
+  border-radius: 50%;
+  background: var(--app-success);
+  box-shadow: 0 0 8px rgba(127, 168, 120, 0.45);
+}
+
+.localStatus {
+  opacity: 0.82;
+}
+
+@media (max-width: 820px) {
+  .contextGroup > span:nth-last-child(-n + 2) {
+    display: none;
+  }
+}
+
+@media (max-width: 620px) {
+  .contextGroup > span:not(:first-child) {
+    display: none;
+  }
+}
diff --git a/frontend/src/components/workbench/WorkbenchShell/StatusBar/StatusBar.tsx b/frontend/src/components/workbench/WorkbenchShell/StatusBar/StatusBar.tsx
new file mode 100644
index 0000000..566302d
--- /dev/null
+++ b/frontend/src/components/workbench/WorkbenchShell/StatusBar/StatusBar.tsx
@@ -0,0 +1,52 @@
+import { Bot, Database, HardDrive, MessageSquareText } from "lucide-react";
+import { useWorkbenchLayout } from "../WorkbenchLayoutContext";
+import styles from "./StatusBar.module.css";
+
+type StatusBarProps = {
+  libraryName: string | null;
+  chatTitle: string | null;
+  agentName: string | null;
+};
+
+export function StatusBar({
+  libraryName,
+  chatTitle,
+  agentName,
+}: StatusBarProps) {
+  const { effectiveDockMode, leftOpen } = useWorkbenchLayout();
+
+  return (
+    <footer className={styles.statusBar} aria-label="Workspace status">
+      <div className={styles.group}>
+        <span className={styles.readyDot} aria-hidden />
+        <span>Ready</span>
+        <span className={styles.localStatus}>
+          <HardDrive size={10} strokeWidth={2} />
+          Local
+        </span>
+      </div>
+
+      <div className={`${styles.group} ${styles.contextGroup}`}>
+        <span title={libraryName ?? "No Library"}>
+          <Database size={10} strokeWidth={2} />
+          {libraryName ?? "No Library"}
+        </span>
+
+        <span title={chatTitle ?? "No Chat"}>
+          <MessageSquareText size={10} strokeWidth={2} />
+          {chatTitle ?? "No Chat"}
+        </span>
+
+        <span title={agentName ?? "No Agent"}>
+          <Bot size={10} strokeWidth={2} />
+          {agentName ?? "No Agent"}
+        </span>
+
+        <span>{leftOpen ? "Explorer open" : "Explorer hidden"}</span>
+        <span>
+          {effectiveDockMode === "attached" ? "Dock attached" : "Dock centered"}
+        </span>
+      </div>
+    </footer>
+  );
+}
diff --git a/frontend/src/components/workbench/WorkbenchShell/WorkbenchLayoutContext.tsx b/frontend/src/components/workbench/WorkbenchShell/WorkbenchLayoutContext.tsx
new file mode 100644
index 0000000..e7911ac
--- /dev/null
+++ b/frontend/src/components/workbench/WorkbenchShell/WorkbenchLayoutContext.tsx
@@ -0,0 +1,27 @@
+import { createContext, useContext } from "react";
+
+export type WorkbenchDockMode = "attached" | "centered";
+
+type WorkbenchLayoutContextValue = {
+  leftOpen: boolean;
+  dockMode: WorkbenchDockMode;
+  effectiveDockMode: WorkbenchDockMode;
+  toggleDockMode: () => void;
+};
+
+const WorkbenchLayoutContext =
+  createContext<WorkbenchLayoutContextValue | null>(null);
+
+export const WorkbenchLayoutProvider = WorkbenchLayoutContext.Provider;
+
+export function useWorkbenchLayout(): WorkbenchLayoutContextValue {
+  const context = useContext(WorkbenchLayoutContext);
+
+  if (!context) {
+    throw new Error(
+      "useWorkbenchLayout must be used inside a WorkbenchLayoutProvider.",
+    );
+  }
+
+  return context;
+}
diff --git a/frontend/src/components/workbench/WorkbenchShell/WorkbenchShell.module.css b/frontend/src/components/workbench/WorkbenchShell/WorkbenchShell.module.css
new file mode 100644
index 0000000..17197e0
--- /dev/null
+++ b/frontend/src/components/workbench/WorkbenchShell/WorkbenchShell.module.css
@@ -0,0 +1,88 @@
+.shell {
+  --workbench-activity-rail-width: 38px;
+  --workbench-statusbar-height: 22px;
+  --workbench-left-panel-compact: clamp(204px, 14vw, 224px);
+  --workbench-left-panel-nested: clamp(220px, 16vw, 250px);
+  --workbench-left-panel-deep: clamp(242px, 18vw, 282px);
+  --workbench-left-panel-width: var(--workbench-left-panel-compact);
+  --workbench-left-overlay-width: calc(
+    var(--workbench-activity-rail-width) + var(--workbench-left-panel-width)
+  );
+  --workbench-surface-bg: #181713;
+  --workbench-chat-safe-left: 0px;
+  --workbench-dock-width: min(calc(100% - 32px), 1120px);
+  --workbench-dock-justify: center;
+  --workbench-dock-margin-left: 16px;
+  --workbench-dock-margin-right: 16px;
+
+  position: fixed;
+  inset: var(--topbar-height) 0 0;
+  display: grid;
+  grid-template-columns: var(--workbench-activity-rail-width) minmax(0, 1fr);
+  grid-template-rows: minmax(0, 1fr) auto var(--workbench-statusbar-height);
+  min-width: 0;
+  min-height: 0;
+  overflow: hidden;
+  color: var(--app-text);
+  background: var(--workbench-surface-bg);
+}
+
+.shell:has(
+  aside[data-workbench-explorer]
+    button[data-tree-level="nested"][aria-expanded="true"]
+) {
+  --workbench-left-panel-width: var(--workbench-left-panel-nested);
+}
+
+.shell:has(
+  aside[data-workbench-explorer]
+    button[data-tree-level="deep"][aria-expanded="true"]
+) {
+  --workbench-left-panel-width: var(--workbench-left-panel-deep);
+}
+
+.dockAttached {
+  --workbench-dock-width: auto;
+  --workbench-dock-justify: stretch;
+  --workbench-dock-margin-left: calc(
+    var(--workbench-left-overlay-width) + 10px
+  );
+  --workbench-dock-margin-right: 12px;
+}
+
+.dockCentered {
+  --workbench-dock-width: min(calc(100% - 32px), 1120px);
+  --workbench-dock-justify: center;
+  --workbench-dock-margin-left: 16px;
+  --workbench-dock-margin-right: 16px;
+}
+
+@media (max-width: 1540px) {
+  .leftOpen {
+    --workbench-chat-safe-left: calc(
+      var(--workbench-activity-rail-width) + 124px
+    );
+  }
+}
+
+@media (max-width: 1120px) {
+  .leftOpen {
+    --workbench-chat-safe-left: var(--workbench-left-overlay-width);
+  }
+
+  .shell {
+    --workbench-left-panel-compact: clamp(196px, 23vw, 218px);
+    --workbench-left-panel-nested: clamp(214px, 26vw, 244px);
+    --workbench-left-panel-deep: clamp(232px, 29vw, 270px);
+  }
+}
+
+@media (max-width: 800px) {
+  .shell {
+    --workbench-chat-safe-left: 0px;
+    --workbench-dock-margin-left: 8px;
+    --workbench-dock-margin-right: 8px;
+    --workbench-dock-width: auto;
+    --workbench-dock-justify: stretch;
+  }
+}
diff --git a/frontend/src/components/workbench/WorkbenchShell/WorkbenchShell.tsx b/frontend/src/components/workbench/WorkbenchShell/WorkbenchShell.tsx
new file mode 100644
index 0000000..864de3b
--- /dev/null
+++ b/frontend/src/components/workbench/WorkbenchShell/WorkbenchShell.tsx
@@ -0,0 +1,136 @@
+import { type ReactNode, useEffect, useMemo, useRef, useState } from "react";
+import { ActivityRail } from "./ActivityRail/ActivityRail";
+import { ExplorerDock } from "./ExplorerDock/ExplorerDock";
+import {
+  type WorkbenchDockMode,
+  WorkbenchLayoutProvider,
+} from "./WorkbenchLayoutContext";
+import { ArtifactDrawer } from "../ArtifactDrawer/ArtifactDrawer";
+import styles from "./WorkbenchShell.module.css";
+
+export type WorkbenchDockView = {
+  id: string;
+  title: string;
+  icon: ReactNode;
+  content: ReactNode;
+};
+
+type WorkbenchShellProps = {
+  leftViews: WorkbenchDockView[];
+  workspace: ReactNode;
+  artifactPanel: ReactNode;
+  statusBar: ReactNode;
+};
+
+type StoredLayout = {
+  activeLeftViewId: string;
+  leftOpen: boolean;
+  dockMode: WorkbenchDockMode;
+};
+
+const STORAGE_KEY = "archivist.workbench.layout.v8";
+
+function readStoredLayout(): StoredLayout | null {
+  try {
+    const rawValue = window.localStorage.getItem(STORAGE_KEY);
+
+    if (!rawValue) {
+      return null;
+    }
+
+    return JSON.parse(rawValue) as StoredLayout;
+  } catch {
+    return null;
+  }
+}
+
+export function WorkbenchShell({
+  leftViews,
+  workspace,
+  artifactPanel,
+  statusBar,
+}: WorkbenchShellProps) {
+  const storedLayout = useRef(readStoredLayout());
+
+  const [activeLeftViewId, setActiveLeftViewId] = useState(
+    storedLayout.current?.activeLeftViewId ?? leftViews[0]?.id ?? "",
+  );
+  const [leftOpen, setLeftOpen] = useState(
+    storedLayout.current?.leftOpen ?? true,
+  );
+  const [dockMode, setDockMode] = useState<WorkbenchDockMode>(
+    storedLayout.current?.dockMode ?? "attached",
+  );
+
+  const effectiveDockMode: WorkbenchDockMode =
+    leftOpen && dockMode === "attached" ? "attached" : "centered";
+
+  useEffect(() => {
+    const nextLayout: StoredLayout = {
+      activeLeftViewId,
+      leftOpen,
+      dockMode,
+    };
+
+    window.localStorage.setItem(STORAGE_KEY, JSON.stringify(nextLayout));
+  }, [activeLeftViewId, dockMode, leftOpen]);
+
+  function activateLeftView(viewId: string) {
+    if (viewId === activeLeftViewId && leftOpen) {
+      setLeftOpen(false);
+      return;
+    }
+
+    setActiveLeftViewId(viewId);
+    setLeftOpen(true);
+  }
+
+  const layoutContext = useMemo(
+    () => ({
+      leftOpen,
+      dockMode,
+      effectiveDockMode,
+      toggleDockMode: () => {
+        setDockMode((current) =>
+          current === "attached" ? "centered" : "attached",
+        );
+      },
+    }),
+    [dockMode, effectiveDockMode, leftOpen],
+  );
+
+  return (
+    <WorkbenchLayoutProvider value={layoutContext}>
+      <div
+        className={`${styles.shell} ${
+          leftOpen ? styles.leftOpen : styles.leftClosed
+        } ${
+          effectiveDockMode === "attached"
+            ? styles.dockAttached
+            : styles.dockCentered
+        }`}
+      >
+        <ActivityRail
+          views={leftViews}
+          activeViewId={activeLeftViewId}
+          panelOpen={leftOpen}
+          onActivateView={activateLeftView}
+        />
+
+        {leftOpen ? (
+          <ExplorerDock
+            views={leftViews}
+            activeViewId={activeLeftViewId}
+            onClose={() => setLeftOpen(false)}
+          />
+        ) : null}
+
+        {workspace}
+
+        <ArtifactDrawer>{artifactPanel}</ArtifactDrawer>
+
+        {statusBar}
+      </div>
+    </WorkbenchLayoutProvider>
+  );
+}
