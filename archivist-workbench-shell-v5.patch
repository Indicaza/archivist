diff --git a/frontend/src/App.tsx b/frontend/src/App.tsx
index a832ba3..6b39524 100644
--- a/frontend/src/App.tsx
+++ b/frontend/src/App.tsx
@@ -8,7 +8,10 @@ import { Chats } from "./components/sidebar/Chats/Chats";
 import { Libraries } from "./components/sidebar/Libraries/Libraries";
 import { LibraryManagementModal } from "./components/sidebar/Libraries/LibraryManagementModal/LibraryManagementModal";
 import { Topbar } from "./components/topbar/Topbar";
-import { ChatPanelToolbar } from "./components/workbench/ChatPanelToolbar";
+import {
+  ChatPanelToolbar,
+  type ChatDockView,
+} from "./components/workbench/ChatPanelToolbar";
 import { DockPlaceholder } from "./components/workbench/DockPlaceholder";
 import { WorkbenchShell } from "./components/workbench/WorkbenchShell";
 import {
@@ -115,6 +118,8 @@ export function App() {
 
   const [restoringManagedChat, setRestoringManagedChat] = useState(false);
 
+  const [chatDockView, setChatDockView] = useState<ChatDockView>(null);
+
   const libraryListItems = useMemo<LibraryListItem[]>(() => {
     return libraries.map((library) => ({
       ...library,
@@ -613,6 +618,38 @@ export function App() {
     }
   }
 
+  async function handleSelectAgent(agentId: string) {
+    const chat = selectedChat;
+
+    if (!chat || chat.agentId === agentId) {
+      return;
+    }
+
+    try {
+      const updatedChat = await editChat(chat.id, {
+        agentId,
+      });
+
+      setChats((current) =>
+        current.map((candidate) =>
+          candidate.id === updatedChat.id ? updatedChat : candidate,
+        ),
+      );
+    } catch (error) {
+      window.alert(
+        error instanceof Error
+          ? error.message
+          : "Archivist could not assign the Agent.",
+      );
+    }
+  }
+
+  function handleToggleChatDockView(
+    nextView: Exclude<ChatDockView, null>,
+  ) {
+    setChatDockView((current) => (current === nextView ? null : nextView));
+  }
+
   async function handleSaveChat(chatId: string, input: UpdateChatInput) {
     setSavingChat(true);
 
@@ -738,7 +775,7 @@ export function App() {
 
   return (
     <>
-      <Topbar selectedLibrary={selectedLibrary} />
+      <Topbar />
 
       <WorkbenchShell
         leftViews={[
@@ -800,45 +837,51 @@ export function App() {
             onChatActivity={handleChatActivity}
             toolbar={
               <ChatPanelToolbar
-                chats={chats}
                 selectedChat={selectedChat}
                 activeAgent={activeAgent}
-                addingChat={addingChat}
-                onSelectChat={handleSelectChat}
-                onAddChat={handleAddChat}
-                onManageChat={setManagedChatId}
-                onManageAgent={setManagedAgentId}
-                chatPanel={
-                  <div className="dock-drawer-stack">
-                    <Chats
-                      chats={chats}
-                      archivedChats={archivedChats}
-                      selectedChatId={selectedChatId}
-                      loading={loadingChats}
-                      adding={addingChat}
-                      onAddChat={handleAddChat}
-                      onSelectChat={handleSelectChat}
-                      onManageChat={setManagedChatId}
-                      onManageArchivedChat={setManagedChatId}
-                    />
-                  </div>
-                }
-                agentPanel={
-                  <div className="dock-drawer-stack">
-                    <Agents
-                      agents={agents}
-                      archivedAgents={archivedAgents}
-                      activeAgentId={activeAgentId}
-                      loading={loadingAgents}
-                      adding={addingAgent}
-                      onAddAgent={handleAddAgent}
-                      onManageAgent={setManagedAgentId}
-                      onManageArchivedAgent={setManagedAgentId}
-                    />
-                  </div>
-                }
+                selectedLibraryName={selectedLibrary?.name ?? null}
+                activeView={chatDockView}
+                onToggleView={handleToggleChatDockView}
               />
             }
+            controlPanelLabel={
+              chatDockView === "chats"
+                ? "Chat browser"
+                : chatDockView === "agents"
+                  ? "Agent browser"
+                  : null
+            }
+            controlPanel={
+              chatDockView === "chats" ? (
+                <div className="dock-drawer-stack">
+                  <Chats
+                    chats={chats}
+                    archivedChats={archivedChats}
+                    selectedChatId={selectedChatId}
+                    loading={loadingChats}
+                    adding={addingChat}
+                    onAddChat={handleAddChat}
+                    onSelectChat={handleSelectChat}
+                    onManageChat={setManagedChatId}
+                    onManageArchivedChat={setManagedChatId}
+                  />
+                </div>
+              ) : chatDockView === "agents" ? (
+                <div className="dock-drawer-stack">
+                  <Agents
+                    agents={agents}
+                    archivedAgents={archivedAgents}
+                    activeAgentId={activeAgentId}
+                    loading={loadingAgents}
+                    adding={addingAgent}
+                    onAddAgent={handleAddAgent}
+                    onSelectAgent={handleSelectAgent}
+                    onManageAgent={setManagedAgentId}
+                    onManageArchivedAgent={setManagedAgentId}
+                  />
+                </div>
+              ) : null
+            }
           />
         }
       />
diff --git a/frontend/src/components/chat/ChatWindow.module.css b/frontend/src/components/chat/ChatWindow.module.css
index f4ede2f..934f15a 100644
--- a/frontend/src/components/chat/ChatWindow.module.css
+++ b/frontend/src/components/chat/ChatWindow.module.css
@@ -214,11 +214,31 @@
   overflow: visible;
 }
 
+
+.composerBody {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr);
+  min-width: 0;
+  min-height: 0;
+  overflow: hidden;
+  border-radius: 0 0 8px 8px;
+}
+
+.composerBodyWithPanel {
+  grid-template-columns: minmax(0, 1fr) minmax(220px, 280px);
+}
+
+.composerBody:not(.composerBodyWithPanel) .composer {
+  border-radius: 0 0 8px 8px;
+}
+
 .composer {
+  display: flex;
   width: 100%;
   min-width: 0;
+  min-height: 0;
   overflow: hidden;
-  border-radius: 0 0 8px 8px;
+  border-radius: 0 0 0 8px;
   background:
     linear-gradient(180deg, rgba(0, 0, 0, 0.08), transparent),
     rgba(16, 15, 13, 0.82);
@@ -339,6 +359,24 @@
   opacity: 0.38;
 }
 
+
+.controlPanel {
+  display: flex;
+  min-width: 0;
+  min-height: 148px;
+  max-height: min(330px, 42vh);
+  overflow: hidden;
+  border-left: 1px solid rgba(214, 198, 170, 0.09);
+  background:
+    linear-gradient(180deg, rgba(255, 255, 255, 0.018), transparent 24%),
+    rgba(12, 11, 10, 0.78);
+}
+
+.controlPanel > * {
+  min-width: 0;
+  min-height: 0;
+}
+
 @keyframes messageIn {
   to {
     opacity: 1;
@@ -379,6 +417,17 @@
   .keyboardHint {
     display: none;
   }
+
+  .composerBodyWithPanel {
+    grid-template-columns: minmax(0, 1fr);
+    grid-template-rows: auto minmax(140px, 190px);
+  }
+
+  .controlPanel {
+    max-height: 190px;
+    border-top: 1px solid rgba(214, 198, 170, 0.09);
+    border-left: 0;
+  }
 }
 
 @media (max-width: 520px) {
diff --git a/frontend/src/components/chat/ChatWindow.tsx b/frontend/src/components/chat/ChatWindow.tsx
index 53d14b1..b99c8ef 100644
--- a/frontend/src/components/chat/ChatWindow.tsx
+++ b/frontend/src/components/chat/ChatWindow.tsx
@@ -22,6 +22,8 @@ import styles from "./ChatWindow.module.css";
 type ChatWindowProps = {
   selectedChat: Chat | null;
   toolbar: ReactNode;
+  controlPanel: ReactNode | null;
+  controlPanelLabel: string | null;
   onChatActivity: (chatId: string) => void;
 };
 
@@ -62,6 +64,8 @@ function createOptimisticUserMessage(
 export function ChatWindow({
   selectedChat,
   toolbar,
+  controlPanel,
+  controlPanelLabel,
   onChatActivity,
 }: ChatWindowProps) {
   const [messages, setMessages] = useState<ChatMessage[]>([]);
@@ -358,74 +362,89 @@ export function ChatWindow({
           <div className={styles.composerToolbar}>{toolbar}</div>
         </header>
 
-        <form className={styles.composer} onSubmit={onSubmit}>
-          <div className={styles.composerInner}>
-            <textarea
-              ref={textareaRef}
-              className={styles.composerInput}
-              value={input}
-              onChange={(event) => setInput(event.target.value)}
-              onKeyDown={onKeyDown}
-              placeholder={
-                selectedChatTitle
-                  ? `Message ${selectedChatTitle}...`
-                  : "Choose or create a chat..."
-              }
-              autoComplete="off"
-              rows={4}
-              disabled={sending || loadingMessages || !selectedChatId}
-            />
-
-            <div className={styles.composerFooter}>
-              <div className={styles.writingTools} aria-label="Writing tools">
-                <button
-                  type="button"
-                  onClick={() => insertTemplate("- ")}
-                  disabled={!selectedChatId || sending}
-                  aria-label="Insert list item"
-                  title="Insert List Item"
-                >
-                  <List size={14} strokeWidth={2} />
-                </button>
+        <div
+          className={`${styles.composerBody} ${
+            controlPanel ? styles.composerBodyWithPanel : ""
+          }`}
+        >
+          <form className={styles.composer} onSubmit={onSubmit}>
+            <div className={styles.composerInner}>
+              <textarea
+                ref={textareaRef}
+                className={styles.composerInput}
+                value={input}
+                onChange={(event) => setInput(event.target.value)}
+                onKeyDown={onKeyDown}
+                placeholder={
+                  selectedChatTitle
+                    ? `Message ${selectedChatTitle}...`
+                    : "Choose or create a chat..."
+                }
+                autoComplete="off"
+                rows={4}
+                disabled={sending || loadingMessages || !selectedChatId}
+              />
+
+              <div className={styles.composerFooter}>
+                <div className={styles.writingTools} aria-label="Writing tools">
+                  <button
+                    type="button"
+                    onClick={() => insertTemplate("- ")}
+                    disabled={!selectedChatId || sending}
+                    aria-label="Insert list item"
+                    title="Insert List Item"
+                  >
+                    <List size={14} strokeWidth={2} />
+                  </button>
+
+                  <button
+                    type="button"
+                    onClick={() => insertTemplate("> ")}
+                    disabled={!selectedChatId || sending}
+                    aria-label="Insert quote"
+                    title="Insert Quote"
+                  >
+                    <TextQuote size={14} strokeWidth={2} />
+                  </button>
+
+                  <button
+                    type="button"
+                    onClick={() => insertTemplate("```\n", "\n```", "code")}
+                    disabled={!selectedChatId || sending}
+                    aria-label="Insert code block"
+                    title="Insert Code Block"
+                  >
+                    <Code2 size={14} strokeWidth={2} />
+                  </button>
+                </div>
 
-                <button
-                  type="button"
-                  onClick={() => insertTemplate("> ")}
-                  disabled={!selectedChatId || sending}
-                  aria-label="Insert quote"
-                  title="Insert Quote"
-                >
-                  <TextQuote size={14} strokeWidth={2} />
-                </button>
+                <span className={styles.keyboardHint}>
+                  Enter to send · Shift+Enter for newline
+                </span>
 
                 <button
-                  type="button"
-                  onClick={() => insertTemplate("```\n", "\n```", "code")}
-                  disabled={!selectedChatId || sending}
-                  aria-label="Insert code block"
-                  title="Insert Code Block"
+                  className={styles.composerButton}
+                  type="submit"
+                  disabled={!canSend}
+                  aria-label="Send"
+                  title="Send"
                 >
-                  <Code2 size={14} strokeWidth={2} />
+                  <SendHorizontal size={16} strokeWidth={2.35} />
+                  <span>Send</span>
                 </button>
               </div>
-
-              <span className={styles.keyboardHint}>
-                Enter to send · Shift+Enter for newline
-              </span>
-
-              <button
-                className={styles.composerButton}
-                type="submit"
-                disabled={!canSend}
-                aria-label="Send"
-                title="Send"
-              >
-                <SendHorizontal size={16} strokeWidth={2.35} />
-                <span>Send</span>
-              </button>
             </div>
-          </div>
-        </form>
+          </form>
+
+          {controlPanel ? (
+            <aside
+              className={styles.controlPanel}
+              aria-label={controlPanelLabel ?? "Chat controls"}
+            >
+              {controlPanel}
+            </aside>
+          ) : null}
+        </div>
       </section>
     </>
   );
diff --git a/frontend/src/components/sidebar/Agents/Agents.tsx b/frontend/src/components/sidebar/Agents/Agents.tsx
index 52224b0..7832446 100644
--- a/frontend/src/components/sidebar/Agents/Agents.tsx
+++ b/frontend/src/components/sidebar/Agents/Agents.tsx
@@ -17,6 +17,7 @@ type AgentsProps = {
   loading: boolean;
   adding: boolean;
   onAddAgent: () => void;
+  onSelectAgent: (agentId: string) => void;
   onManageAgent: (agentId: string) => void;
   onManageArchivedAgent: (agentId: string) => void;
 };
@@ -28,6 +29,7 @@ export function Agents({
   loading,
   adding,
   onAddAgent,
+  onSelectAgent,
   onManageAgent,
   onManageArchivedAgent,
 }: AgentsProps) {
@@ -60,8 +62,8 @@ export function Agents({
                   <button
                     className={`${styles.row} ${selected ? styles.selected : ""}`}
                     type="button"
-                    onClick={() => onManageAgent(agent.id)}
-                    title={agent.name}
+                    onClick={() => onSelectAgent(agent.id)}
+                    title={`Use ${agent.name}`}
                   >
                     {agent.isBuiltIn ? (
                       <ShieldCheck size={13} strokeWidth={2} />
diff --git a/frontend/src/components/sidebar/Sidebar.tsx b/frontend/src/components/sidebar/Sidebar.tsx
index 099228d..f67f1a2 100644
--- a/frontend/src/components/sidebar/Sidebar.tsx
+++ b/frontend/src/components/sidebar/Sidebar.tsx
@@ -1,5 +1,5 @@
 import type { ReactNode } from "react";
-import { ChevronsRight, PanelLeftClose } from "lucide-react";
+import { PanelLeftClose } from "lucide-react";
 import styles from "./Sidebar.module.css";
 
 export type SidebarView = {
@@ -12,18 +12,14 @@ export type SidebarView = {
 type SidebarProps = {
   views: SidebarView[];
   activeViewId: string;
-  sizeLabel: string;
   onActivateView: (viewId: string) => void;
-  onCycleSize: () => void;
   onClosePanel: () => void;
 };
 
 export function Sidebar({
   views,
   activeViewId,
-  sizeLabel,
   onActivateView,
-  onCycleSize,
   onClosePanel,
 }: SidebarProps) {
   const activeView =
@@ -61,18 +57,6 @@ export function Sidebar({
             })}
           </nav>
 
-          <span className={styles.divider} />
-
-          <button
-            className={styles.headerButton}
-            type="button"
-            onClick={onCycleSize}
-            aria-label={`Resize Explorer. Current size: ${sizeLabel}`}
-            title={`Explorer size: ${sizeLabel}. Click for next size.`}
-          >
-            <ChevronsRight size={15} strokeWidth={2} />
-          </button>
-
           <button
             className={styles.headerButton}
             type="button"
diff --git a/frontend/src/components/topbar/Topbar.tsx b/frontend/src/components/topbar/Topbar.tsx
index 20ef068..3f146bc 100644
--- a/frontend/src/components/topbar/Topbar.tsx
+++ b/frontend/src/components/topbar/Topbar.tsx
@@ -1,27 +1,17 @@
-import type { LibraryListItem } from "../../domains/library/library.types";
-import { ActiveLibraryBadge } from "./ActiveLibraryBadge/ActiveLibraryBadge";
 import styles from "./Topbar.module.css";
 
-type TopbarProps = {
-  selectedLibrary: LibraryListItem | null;
-};
-
-export function Topbar({ selectedLibrary }: TopbarProps) {
+export function Topbar() {
   return (
-    <>
-      <header className={styles.topbar}>
-        <div className={styles.topbarContainer}>
-          <div className={styles.topbarLeft}>
-            <h1 className={styles.topbarTitle}>Archivist</h1>
-          </div>
-
-          <div className={styles.topbarRight}>
-            <button className={styles.userAvatar} aria-label="User profile" />
-          </div>
+    <header className={styles.topbar}>
+      <div className={styles.topbarContainer}>
+        <div className={styles.topbarLeft}>
+          <h1 className={styles.topbarTitle}>Archivist</h1>
         </div>
-      </header>
 
-      <ActiveLibraryBadge selectedLibrary={selectedLibrary} />
-    </>
+        <div className={styles.topbarRight}>
+          <button className={styles.userAvatar} aria-label="User profile" />
+        </div>
+      </div>
+    </header>
   );
 }
diff --git a/frontend/src/components/workbench/ChatPanelToolbar.module.css b/frontend/src/components/workbench/ChatPanelToolbar.module.css
index 646420d..8c78787 100644
--- a/frontend/src/components/workbench/ChatPanelToolbar.module.css
+++ b/frontend/src/components/workbench/ChatPanelToolbar.module.css
@@ -1,222 +1,119 @@
 .toolbar {
-  position: relative;
   display: flex;
-  align-items: center;
+  align-items: stretch;
   justify-content: space-between;
+  width: 100%;
   min-width: 0;
   gap: 8px;
-  width: 100%;
 }
 
-.primaryControls,
-.drawerTabs {
+.contextSummary,
+.panelTabs {
   display: flex;
-  align-items: center;
+  align-items: stretch;
   min-width: 0;
-  gap: 4px;
 }
 
-.primaryControls {
+.contextSummary {
   flex: 1;
   overflow: hidden;
 }
 
-.drawerTabs {
-  flex: 0 0 auto;
-  align-self: stretch;
-}
-
-.selectWrap {
+.contextItem {
   display: flex;
   align-items: center;
-  flex: 1 1 190px;
-  min-width: 120px;
-  max-width: 300px;
-  height: 28px;
+  min-width: 0;
+  max-width: 240px;
   gap: 5px;
-  padding: 0 6px;
-  border: 1px solid rgba(214, 198, 170, 0.12);
-  border-radius: 5px;
+  padding: 0 9px;
+  border-right: 1px solid rgba(214, 198, 170, 0.08);
   color: var(--app-muted-text);
-  background: rgba(0, 0, 0, 0.16);
-}
-
-.select {
-  min-width: 0;
-  flex: 1;
-  border: 0;
-  outline: 0;
-  color: var(--app-text);
-  background: transparent;
-  font-size: 11px;
-  cursor: pointer;
+  font-size: 10px;
 }
 
-.iconButton,
-.agentButton,
-.drawerTab {
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  height: 28px;
-  padding: 0;
-  border: 1px solid transparent;
-  border-radius: 5px;
-  color: var(--app-muted-text);
-  background: transparent;
-  cursor: pointer;
+.contextItem svg {
+  flex: 0 0 auto;
 }
 
-.iconButton {
-  width: 28px;
+.contextLabel {
   flex: 0 0 auto;
+  color: var(--app-muted-text);
+  font-size: 8px;
+  font-weight: 700;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  opacity: 0.72;
 }
 
-.agentButton {
+.contextItem strong {
   min-width: 0;
-  max-width: 190px;
-  gap: 6px;
-  padding: 0 8px;
-  font-size: 11px;
-}
-
-.agentButton span,
-.drawerTab span {
   overflow: hidden;
+  color: var(--app-text);
+  font-weight: 600;
   text-overflow: ellipsis;
   white-space: nowrap;
 }
 
-.drawerTab {
-  align-self: stretch;
-  height: auto;
-  gap: 5px;
-  padding: 0 8px;
-  border-radius: 0;
-  font-size: 10px;
-  font-weight: 650;
-}
-
-.drawerTabActive {
-  border-bottom-color: var(--app-accent);
-  color: var(--app-text);
-  background: rgba(143, 122, 223, 0.08);
-}
-
-.iconButton:hover:not(:disabled),
-.iconButton:focus-visible:not(:disabled),
-.agentButton:hover:not(:disabled),
-.agentButton:focus-visible:not(:disabled),
-.drawerTab:hover,
-.drawerTab:focus-visible {
-  border-color: rgba(143, 122, 223, 0.25);
-  color: var(--app-text);
-  background: rgba(255, 255, 255, 0.045);
-  outline: none;
-}
-
-.iconButton:disabled,
-.agentButton:disabled {
-  cursor: default;
-  opacity: 0.42;
-}
-
-.divider {
-  width: 1px;
-  height: 18px;
-  margin: 0 2px;
-  background: rgba(214, 198, 170, 0.1);
-}
-
-.drawer {
-  position: absolute;
-  z-index: 12;
-  right: -5px;
-  bottom: calc(100% + 8px);
-  display: grid;
-  grid-template-rows: 31px minmax(0, 1fr);
-  width: clamp(250px, 24vw, 360px);
-  height: min(470px, 56vh);
-  min-height: 240px;
-  overflow: hidden;
-  border: 1px solid var(--sidebar-border);
-  border-radius: 8px 8px 0 8px;
-  background:
-    linear-gradient(180deg, rgba(255, 250, 240, 0.018), transparent 24%),
-    rgba(16, 15, 13, 0.98);
-  box-shadow: 0 20px 54px rgba(0, 0, 0, 0.58);
-  contain: layout paint style;
+.panelTabs {
+  flex: 0 0 auto;
 }
 
-.drawerHeader {
-  display: flex;
+.panelTab {
+  display: inline-flex;
   align-items: center;
-  justify-content: space-between;
-  padding: 0 5px 0 9px;
-  border-bottom: 1px solid rgba(214, 198, 170, 0.09);
-  color: var(--app-muted-text);
-  font-size: 9px;
-  font-weight: 700;
-  letter-spacing: 0.1em;
-  text-transform: uppercase;
-}
-
-.drawerHeader button {
-  display: grid;
-  place-items: center;
-  width: 24px;
-  height: 24px;
-  padding: 0;
+  justify-content: center;
+  align-self: stretch;
+  gap: 5px;
+  min-width: 66px;
+  padding: 0 9px;
   border: 0;
-  border-radius: 4px;
+  border-left: 1px solid rgba(214, 198, 170, 0.07);
+  border-bottom: 2px solid transparent;
   color: var(--app-muted-text);
   background: transparent;
+  font: inherit;
+  font-size: 10px;
+  font-weight: 650;
   cursor: pointer;
 }
 
-.drawerHeader button:hover,
-.drawerHeader button:focus-visible {
+.panelTab:hover,
+.panelTab:focus-visible {
   color: var(--app-text);
-  background: rgba(255, 255, 255, 0.055);
+  background: rgba(255, 255, 255, 0.045);
   outline: none;
 }
 
-.drawerBody {
-  display: flex;
-  min-width: 0;
-  min-height: 0;
-  overflow: hidden;
-}
-
-.drawerBody > * {
-  min-height: 0;
+.panelTabActive {
+  border-bottom-color: var(--app-accent);
+  color: var(--app-text);
+  background: rgba(143, 122, 223, 0.08);
 }
 
-@media (max-width: 760px) {
-  .selectWrap {
-    min-width: 100px;
-    max-width: 190px;
-  }
-
-  .agentButton {
-    max-width: 118px;
+@media (max-width: 880px) {
+  .contextItem {
+    max-width: 170px;
+    padding-right: 6px;
+    padding-left: 6px;
   }
 
-  .drawer {
-    width: min(330px, calc(100vw - 34px));
+  .contextLabel {
+    display: none;
   }
 }
 
-@media (max-width: 560px) {
-  .agentButton span,
-  .divider,
-  .drawerTab span {
+@media (max-width: 620px) {
+  .contextItem:nth-child(3) {
     display: none;
   }
 
-  .agentButton,
-  .drawerTab {
-    width: 28px;
+  .panelTab {
+    min-width: 30px;
+    width: 30px;
     padding: 0;
   }
+
+  .panelTab span {
+    display: none;
+  }
 }
diff --git a/frontend/src/components/workbench/ChatPanelToolbar.tsx b/frontend/src/components/workbench/ChatPanelToolbar.tsx
index f4641a1..75fa542 100644
--- a/frontend/src/components/workbench/ChatPanelToolbar.tsx
+++ b/frontend/src/components/workbench/ChatPanelToolbar.tsx
@@ -1,144 +1,64 @@
-import { useEffect, useRef, useState } from "react";
-import type { MouseEvent as ReactMouseEvent, ReactNode } from "react";
-import {
-  Bot,
-  MessageSquareText,
-  Pencil,
-  Plus,
-  X,
-} from "lucide-react";
+import { Bot, FolderOpen, MessageSquareText } from "lucide-react";
 import type { Agent } from "../../domains/agent/agent.types";
 import type { Chat } from "../../domains/chat/chat.types";
 import styles from "./ChatPanelToolbar.module.css";
 
-type DrawerView = "chats" | "agents" | null;
+export type ChatDockView = "chats" | "agents" | null;
 
 type ChatPanelToolbarProps = {
-  chats: Chat[];
   selectedChat: Chat | null;
   activeAgent: Agent | null;
-  addingChat: boolean;
-  chatPanel: ReactNode;
-  agentPanel: ReactNode;
-  onSelectChat: (chatId: string) => void;
-  onAddChat: () => void;
-  onManageChat: (chatId: string) => void;
-  onManageAgent: (agentId: string) => void;
+  selectedLibraryName: string | null;
+  activeView: ChatDockView;
+  onToggleView: (view: Exclude<ChatDockView, null>) => void;
 };
 
 export function ChatPanelToolbar({
-  chats,
   selectedChat,
   activeAgent,
-  addingChat,
-  chatPanel,
-  agentPanel,
-  onSelectChat,
-  onAddChat,
-  onManageChat,
-  onManageAgent,
+  selectedLibraryName,
+  activeView,
+  onToggleView,
 }: ChatPanelToolbarProps) {
-  const [drawerView, setDrawerView] = useState<DrawerView>(null);
-  const rootRef = useRef<HTMLDivElement | null>(null);
-
-  useEffect(() => {
-    function handlePointerDown(event: MouseEvent) {
-      if (!rootRef.current?.contains(event.target as Node)) {
-        setDrawerView(null);
-      }
-    }
-
-    function handleKeyDown(event: KeyboardEvent) {
-      if (event.key === "Escape") {
-        setDrawerView(null);
-      }
-    }
-
-    document.addEventListener("mousedown", handlePointerDown);
-    document.addEventListener("keydown", handleKeyDown);
-
-    return () => {
-      document.removeEventListener("mousedown", handlePointerDown);
-      document.removeEventListener("keydown", handleKeyDown);
-    };
-  }, []);
-
-  function toggleDrawer(
-    event: ReactMouseEvent<HTMLButtonElement>,
-    nextView: Exclude<DrawerView, null>,
-  ) {
-    event.stopPropagation();
-    setDrawerView((current) => (current === nextView ? null : nextView));
-  }
-
-  const drawerTitle = drawerView === "chats" ? "Chats" : "Agents";
-
   return (
-    <div ref={rootRef} className={styles.toolbar}>
-      <div className={styles.primaryControls}>
-        <label className={styles.selectWrap}>
-          <MessageSquareText size={13} strokeWidth={2.1} />
-
-          <select
-            className={styles.select}
-            value={selectedChat?.id ?? ""}
-            onChange={(event) => onSelectChat(event.target.value)}
-            aria-label="Active chat"
-          >
-            {!selectedChat ? <option value="">Choose a chat</option> : null}
-
-            {chats.map((chat) => (
-              <option key={chat.id} value={chat.id}>
-                {chat.title}
-              </option>
-            ))}
-          </select>
-        </label>
-
-        <button
-          className={styles.iconButton}
-          type="button"
-          onClick={onAddChat}
-          disabled={addingChat}
-          aria-label="Create chat"
-          title="Create Chat"
+    <div className={styles.toolbar}>
+      <div className={styles.contextSummary} aria-label="Active chat context">
+        <div
+          className={styles.contextItem}
+          title={selectedChat?.title ?? "No active Chat"}
         >
-          <Plus size={15} strokeWidth={2.2} />
-        </button>
+          <MessageSquareText size={13} strokeWidth={2.1} />
+          <span className={styles.contextLabel}>Chat</span>
+          <strong>{selectedChat?.title ?? "None"}</strong>
+        </div>
 
-        <button
-          className={styles.iconButton}
-          type="button"
-          onClick={() => selectedChat && onManageChat(selectedChat.id)}
-          disabled={!selectedChat}
-          aria-label="Manage active chat"
-          title="Manage Chat"
+        <div
+          className={styles.contextItem}
+          title={activeAgent?.name ?? "No active Agent"}
         >
-          <Pencil size={14} strokeWidth={2.1} />
-        </button>
-
-        <span className={styles.divider} />
+          <Bot size={13} strokeWidth={2.1} />
+          <span className={styles.contextLabel}>Agent</span>
+          <strong>{activeAgent?.name ?? "None"}</strong>
+        </div>
 
-        <button
-          className={styles.agentButton}
-          type="button"
-          onClick={() => activeAgent && onManageAgent(activeAgent.id)}
-          disabled={!activeAgent}
-          title={activeAgent ? `Manage ${activeAgent.name}` : "No active Agent"}
+        <div
+          className={styles.contextItem}
+          title={selectedLibraryName ?? "Standalone workspace"}
         >
-          <Bot size={14} strokeWidth={2.1} />
-          <span>{activeAgent?.name ?? "No Agent"}</span>
-        </button>
+          <FolderOpen size={13} strokeWidth={2.1} />
+          <span className={styles.contextLabel}>Library</span>
+          <strong>{selectedLibraryName ?? "Standalone"}</strong>
+        </div>
       </div>
 
-      <nav className={styles.drawerTabs} aria-label="Chat workspace controls">
+      <nav className={styles.panelTabs} aria-label="Chat dock panels">
         <button
-          className={`${styles.drawerTab} ${
-            drawerView === "chats" ? styles.drawerTabActive : ""
+          className={`${styles.panelTab} ${
+            activeView === "chats" ? styles.panelTabActive : ""
           }`}
           type="button"
-          onClick={(event) => toggleDrawer(event, "chats")}
-          aria-expanded={drawerView === "chats"}
+          onClick={() => onToggleView("chats")}
+          aria-expanded={activeView === "chats"}
           title="Browse Chats"
         >
           <MessageSquareText size={13} strokeWidth={2.1} />
@@ -146,39 +66,18 @@ export function ChatPanelToolbar({
         </button>
 
         <button
-          className={`${styles.drawerTab} ${
-            drawerView === "agents" ? styles.drawerTabActive : ""
+          className={`${styles.panelTab} ${
+            activeView === "agents" ? styles.panelTabActive : ""
           }`}
           type="button"
-          onClick={(event) => toggleDrawer(event, "agents")}
-          aria-expanded={drawerView === "agents"}
+          onClick={() => onToggleView("agents")}
+          aria-expanded={activeView === "agents"}
           title="Browse Agents"
         >
           <Bot size={13} strokeWidth={2.1} />
           <span>Agents</span>
         </button>
       </nav>
-
-      {drawerView ? (
-        <aside className={styles.drawer} aria-label={`${drawerTitle} controls`}>
-          <header className={styles.drawerHeader}>
-            <span>{drawerTitle}</span>
-
-            <button
-              type="button"
-              onClick={() => setDrawerView(null)}
-              aria-label={`Close ${drawerTitle}`}
-              title={`Close ${drawerTitle}`}
-            >
-              <X size={14} strokeWidth={2.1} />
-            </button>
-          </header>
-
-          <div className={styles.drawerBody}>
-            {drawerView === "chats" ? chatPanel : agentPanel}
-          </div>
-        </aside>
-      ) : null}
     </div>
   );
 }
diff --git a/frontend/src/components/workbench/WorkbenchShell.module.css b/frontend/src/components/workbench/WorkbenchShell.module.css
index e3f5d77..8db2df1 100644
--- a/frontend/src/components/workbench/WorkbenchShell.module.css
+++ b/frontend/src/components/workbench/WorkbenchShell.module.css
@@ -1,5 +1,5 @@
 .shell {
-  --workbench-left-panel-width: 272px;
+  --workbench-left-panel-width: clamp(252px, 21vw, 348px);
   --workbench-left-panel-space: var(--workbench-left-panel-width);
 
   position: fixed;
@@ -14,18 +14,6 @@
   background: var(--workspace-bg);
 }
 
-.compact {
-  --workbench-left-panel-width: clamp(250px, 18vw, 286px);
-}
-
-.comfortable {
-  --workbench-left-panel-width: clamp(310px, 23vw, 360px);
-}
-
-.wide {
-  --workbench-left-panel-width: clamp(390px, 29vw, 460px);
-}
-
 .leftClosed {
   --workbench-left-panel-space: 0px;
 }
@@ -56,6 +44,12 @@
   outline: none;
 }
 
+@media (max-width: 980px) {
+  .shell {
+    --workbench-left-panel-width: clamp(244px, 30vw, 318px);
+  }
+}
+
 @media (max-width: 800px) {
   .shell {
     --workbench-left-panel-space: 0px;
diff --git a/frontend/src/components/workbench/WorkbenchShell.tsx b/frontend/src/components/workbench/WorkbenchShell.tsx
index 599d847..f179dff 100644
--- a/frontend/src/components/workbench/WorkbenchShell.tsx
+++ b/frontend/src/components/workbench/WorkbenchShell.tsx
@@ -12,16 +12,12 @@ type WorkbenchShellProps = {
   artifactPanel: ReactNode;
 };
 
-type LeftPanelSize = "compact" | "comfortable" | "wide";
-
 type StoredLayout = {
   activeLeftViewId: string;
   leftOpen: boolean;
-  leftPanelSize: LeftPanelSize;
 };
 
-const STORAGE_KEY = "archivist.workbench.layout.v4";
-const panelSizes: LeftPanelSize[] = ["compact", "comfortable", "wide"];
+const STORAGE_KEY = "archivist.workbench.layout.v5";
 
 function readStoredLayout(): StoredLayout | null {
   try {
@@ -37,13 +33,6 @@ function readStoredLayout(): StoredLayout | null {
   }
 }
 
-function getNextPanelSize(currentSize: LeftPanelSize): LeftPanelSize {
-  const currentIndex = panelSizes.indexOf(currentSize);
-  const nextIndex = (currentIndex + 1) % panelSizes.length;
-
-  return panelSizes[nextIndex] ?? "compact";
-}
-
 export function WorkbenchShell({
   leftViews,
   workspace,
@@ -57,19 +46,15 @@ export function WorkbenchShell({
   const [leftOpen, setLeftOpen] = useState(
     storedLayout.current?.leftOpen ?? true,
   );
-  const [leftPanelSize, setLeftPanelSize] = useState<LeftPanelSize>(
-    storedLayout.current?.leftPanelSize ?? "compact",
-  );
 
   useEffect(() => {
     const nextLayout: StoredLayout = {
       activeLeftViewId,
       leftOpen,
-      leftPanelSize,
     };
 
     window.localStorage.setItem(STORAGE_KEY, JSON.stringify(nextLayout));
-  }, [activeLeftViewId, leftOpen, leftPanelSize]);
+  }, [activeLeftViewId, leftOpen]);
 
   function activateLeftView(viewId: string) {
     if (viewId === activeLeftViewId && leftOpen) {
@@ -80,23 +65,17 @@ export function WorkbenchShell({
     setLeftOpen(true);
   }
 
-  function cycleLeftPanelSize() {
-    setLeftPanelSize((currentSize) => getNextPanelSize(currentSize));
-  }
-
   return (
     <div
       className={`${styles.shell} ${
         leftOpen ? styles.leftOpen : styles.leftClosed
-      } ${styles[leftPanelSize]}`}
+      }`}
     >
       {leftOpen ? (
         <Sidebar
           views={leftViews}
           activeViewId={activeLeftViewId}
-          sizeLabel={leftPanelSize}
           onActivateView={activateLeftView}
-          onCycleSize={cycleLeftPanelSize}
           onClosePanel={() => setLeftOpen(false)}
         />
       ) : (
